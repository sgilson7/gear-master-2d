# SECOND-ORDER-M16.md — the notebook

*Written while the work is done, not at the end of it. M13's convention, kept
because it paid for itself: seven of M13's rows were marked as candidates, all
seven were executed in M13.9, and three of them turned up something that was
actually wrong.*

**Marked `worklist` is a row the last milestone of the combined block executes.**

---

## Rows

| # | row | kind | status |
|---|---|---|---|
| 1 | **The save did not arrive.** `PROMPT-M16.md` says to place `testing/saves/the-run-20260910.json` before starting and to treat it as the human's own file. It was not in the tree. `PLAN-M16.md` §5.1 transcribes the board in full, so the fixture is **reconstructed** from the transcription by `crates/lab/src/mkrun.rs` and checked in. `the_save_opens_at_forty_five` and `the_tenth_surveyor_wears_the_run` are what make that honest: if the transcription and the reconstruction disagree, two tests say so. | divergence | done (M16.0) |
| 2 | **A board's placement order is not item order, so `MonsterSpec.items` cannot be written over §5.1's list.** The run's first helmet item is gear entries 0, 3 and 5 and its second is 1, 2 and 4 — interleaved. `Character::item_partition` therefore returns **the gear in item order and the chunk list**, not the chunk list alone. Reordering is free: a placement carries its own absolute cell. | divergence | done (M16.0) |
| 3 | **Three of the run's weapon pieces cannot be in an assembled item.** The bottom row of §5.1's weapon board is Accessory + Ink + Alignment with no core, so it assembles under no locking discipline. The search over all 4,096 lock masks tops out at 9 of 12 on that grid; the other four grids are 6/6, 6/6, 8/8 and 6/6. Eleven assembled items out of twelve groups. | finding | done (M16.0) |
| 4 | **`PLAN-M16.md` §5.3's curse-cap numbers do not reproduce, in either direction.** The plan says two creatures are past the cap, the Ninth Surveyor at 114 and What Marbulon Faced Away From at 120. Measured: the Ninth Surveyor is at **88** — under it — Marbulon is at **102**, and **twenty-three** creatures are past, up to Nine of Ashes at 233. | finding | done (M16.0) |
| 5 | **And nobody had asked the mind lane the same question.** Thirty-one of the sixty creatures are at or past a hundred mind resist, **every deep boss among them** — Marbulon 128, the Ninth Surveyor 126, The Rust Parliament 200, Gilt 190. M16.4 adds a base class whose whole promise is that lane. The fix is `stats::LANE_CAP` at 95, which is `RESIST_CAP`'s own argument applied to the two lanes that now have classes behind them — **not** the plan's re-dressing, because re-dressing a quarter of the ladder to move a constant is *ease a pool, not a creature* two levels up. Nothing under 95 moved. | divergence | done (M16.0) |
| 6 | **Three unit tests pinned "fully resisted, never lands"** and were updated rather than deleted, with the reason at each. A fourth thing to watch: `explain::defences_of` now prints three ceilings where it printed two. | finding | done (M16.0) |
| 7 | `crates/lab/src/probe.rs` is a scratch recon binary. It should either earn a name and a doc comment or be deleted before the block closes. **It earned one: `fan.rs`**, and it measures what `SECOND-ORDER-M17.md` row 3 says is the number that means something for a cue — how many distinct tiles a shot can reach, rather than mean distance, which saturates by construction. It takes a map id, because there are two tables now. | worklist | done (M18.0) |
| 8 | **§4.1's and §4.2's ASCII rows are not all the same width** — floor one's mix 14 and 15, floor two's mix 14 and 15, floor three's mix 14 and 15. Redrawn at a consistent width in each case: 16×15, 16×8, 15×12. The rooms and necks the annotations name are all preserved. | divergence | done (M16.1) |
| 9 | **`solvable_blind` on the Flat Below is 36, and §6 asks for 9.** Nine is the plan's own §4.1 count of *pulls*; `solvable_blind` counts **card reads**, which is M14's unit and the unit the Cairnfield's 45 is in. Nine stakes in three gated groups is 36, not the 18 a first estimate gives, because the adversary opens each band early and leaves its two wrong neighbours live. Both numbers are asserted, each in its own unit. | divergence | done (M16.1) |
| 10 | **The atlas cannot be knowledge the engine does not read.** §4.1 has the atlas raise `read-band-x` on each stake so *the card's prose then says the one on the right*; prose is static and `every_flag_an_event_sets_is_read_by_something` refuses a flag nothing consults — the `Outcome::Xp` bug. So the reading is on the **sheet**, where a survey belongs, and it opens the three bays that hold: one card instead of three stakes. The plan's fifteen fatigue is charged, in one go. | divergence | done (M16.1) |
| 11 | **A no-op choice can be behind a requirement, and `no_choice_that_does_nothing_spends_a_door` did not know it.** The compass reading is `Outcome::Nothing` and wants an instrument, so a fresh game could not take it and the lint failed on the refusal rather than on the rule. `common::with_instrument` is the fixture that fixes it — the parts read off the recipe table, never a list of names. | finding | done (M16.1) |
| 12 | The pockets are `PlaceKind::Boss` with no drops, so a pocket is a **certainty** rather than a 260‰ roll. Six boss tiles on one floor takes the game's boss-tile count from 9 to 15, which M16.3's *the boss refusal is the tile's and not the name's* rule (`fight::instant`) now applies to. **Checked, and it does not.** Two halves, and both are `a_pocket_is_a_boss_tile_and_cannot_be_farmed_for_a_mark`: a pocket refuses exactly as the plate at the bottom does, because it is a tile; and the whole floor is worth at most **two** of the five wins a mark costs, because three creatures are in two pockets each and each pocket is spent the first time. A player who has marked the Iron Abbot marked it in the field, which is what the mark means. | worklist | done (M18.1) |
| 13 | **No board this game hands a player reaches Rare, and `PLAN-M16.md` §4.2 hangs two doors above it.** Measured: the run as the human left it tops out at item rating **50**; Auto-packed out of everything it owns, still 50; `RARE_AT` is 90, `EPIC_AT` 130, `LEGENDARY_AT` 170, and 562 of the 568 components rate Common on their own. `geared_from` — both shelves and every errand — reaches **135, Epic**. So door one at `epic` is answerable by a mid-game shopper and **not** by the level-45 run, and door three at `legendary` is answerable by nothing in the game: the best item on the whole creature ladder is Francis's at 343, and a player's board is what they could buy. The Assay asks for footprints and money instead, and keeps `epic` on door one as the *shortcut* rather than the lock. `no_board_a_player_can_build_reaches_rare` is the lint. | divergence | done (M16.2) |
| 14 | **A level-45 caster board is Common and a level-10 shopper's is Epic**, because `item_rating` prices pieces and cadence and an Ink is a multiplier that rates nothing on its own. That is a live question for the human: either `Rarity`'s thresholds are wrong for player boards, or a caster board should rate differently, or rarity is a creature's measure and not a player's. **Nothing in this block turns on it** — the Assay was rewritten around it — and the Shelf's shipped `epic` door is passable by `geared_from`, so nothing is broken today. **Escalated rather than decided**, because all three answers are content calls and none of them is the builder's: moving `RARE_AT` retunes every creature's tier, making an Ink rate changes what `item_rating` is *for*, and declaring rarity a creature's measure is a decision about a word players read. `no_board_a_player_can_build_reaches_rare` is the lint that keeps it from being rediscovered, and it will fail the day somebody moves a threshold without meaning to. | worklist | **the human's** |
| 15 | **`every_drain_names_terrain_that_exists` asks a question that predates `Drain.tiles`.** Its corner clause refuses a drain whose `from` is the corner's terrain, on the grounds that it is a whole-map rewrite — true of a drain with no `tiles` and false of one that names three cells. The Assay's three doors are one tile of rock each. Sharpened to ask only of whole-map drains; the loader refuses the other case anyway, so it is defence in depth. | finding | done (M16.2) |
| 16 | **`an_event_that_asks_something_still_reopens` marked the event answered and not the place present.** A card behind a `hidden_until` is not on the map, so stepping onto its tile reports nothing and the check read that as a card that had stopped reporting itself. The two had never come apart before the Assay's third door. Fixed by marking whatever makes the place there. | finding | done (M16.2) |
| 17 | **`cargo test --workspace` is ten minutes after any `data/` change**, because every map file is an `include_str!` and sixty test binaries relink. `SECOND-ORDER-M14.md` row 17 says the same of `combat.rs`. Worth measuring whether the data files could be loaded rather than embedded in the *test* profile only. **Measured: 127 seconds relinking and 47 running, so a `data/` touch costs about three minutes** — 83 test binaries, on an idle machine. The row's *ten minutes* is a cold `--workspace`, which adds the lab and the shim on top of both. So `include_str!` is not the thing to change — and it could not be anyway without the tested code path differing from the shipped one, which is two rulebooks. The fix if one is ever wanted is **fewer test binaries**, a trade against one file per concern that nobody should make to save two minutes. | worklist | done (M18.6) |
| 18 | **The bracket in §5.2 is not measurable, because combat has no RNG.** *"the save should beat her between 55% and 70% of the time at Medium … `combat::simulate` is deterministic, so the bracket is a loop over seeds and a count"* — a loop over seeds counts the same fight every time: `CLAUDE.md`'s first combat rule is that there is no RNG, which is why a mid-fight save carries a creature name and a tile and nothing else. What varies between two players meeting her is the **board**, so the bracket is over boards: the run beats her and `geared_from` does not, which is M11.7's *the board a player actually has*, stated twice. | divergence | M16.3 |
| 19 | **The fixture's instrument frame is empty**, because §5.1 deliberately does not transcribe one — *the instrument frame is not carried, a creature has no survey.* That is right for the boss and it means the saved run cannot open `the-way-under-the-flat`, which wants an instrument. A fixture limitation, not a game fact: the human's real run must have had one, since the Sump's lip wants one too. Anything in this block that needs the run to be *in* the Sands plants the instrument with `common::with_instrument`. | finding | M16.3 |
| 20 | **`Outcome::Warp` is invisible to `puzzle::reachable`**, which floods terrain only. The Needle Room's alcoves are reachable by nothing else, so the floor measured `Stuck`. `reachable` follows a warp on an unconditional choice now, to a fixpoint — which is the general fix rather than the instance, and the plan's fallback (`needs` gates keyed to each sinkhole's flag) is not needed. | divergence | M16.3 |
| 21 | **The sinkholes do not repeat, and §4.3 says they should.** `a_repeating_event_may_never_pay` refuses a repeating warp, for a reason that is still right — *a warp is a free ride and the Drover's Stride costs a tin.* They do not need to: both ungated levers sit behind a true sinkhole, each lever drains the way out of its own alcove, so each hole is needed exactly once and no order strands anybody. What `repeats` would buy is the ability to pay eight fatigue twice. | divergence | M16.3 |
| 22 | The sharper rule the row above dodges: **a warp that stays on the floor you are standing on is a fall, not a free ride**, and `pays()` cannot tell the two apart because `events.json` does not know which map an event is on. A map-aware lint could, and now does: `a_warp_that_stays_on_its_own_map_is_a_fall_and_not_a_ride` joins `events.json` to the maps that place its events. **Four falls and one ride**, so the distinction is real content rather than theory — the conservative parse rule is costing four events the ability to repeat, and each is a hole you can fall into once. The lint says out loud which of its two halves is live: the classification is, and *a ride may not repeat* is shadowed by parse and written down for the day somebody relaxes it. | worklist | done (M18.2) |
| 23 | **`geared_from` is twice the board the reconstruction is** — 1,942 health and 21 items against the run's 974 and 11 — and it beats both M14 bosses while the run loses to both. The run's gear is cheap early components (Bone Crown, Tin Band, Plain Sole) packed into a caster board, so *"an almost finished run that is very powerful"* does not describe what §5.1 transcribes. Either the human's run was a scrappy caster build or §5.1 is a picture of an earlier save. **The Tenth Surveyor is therefore bracketed against `geared_from`**, which is M11.7's rule and M14's, and the run losing to a boss wearing its own gear is in register rather than a failure. | finding | done (M16.3) |
| 24 | **Her rating (2,413) is mostly her health**, because `item_rating` prices pieces and cadence and her pieces are a player's cheap ones — while her *teeth* are six enchs, which rating deliberately does not count (*an ench does not move what an item is worth*). At her first draft's 236 strength she dealt 807.9/s and rated 2,351. A creature whose rating under-describes it by that much is a live question for the human: it is what `xp_for` and `draw_enemy` read, and she is a boss on a tile so neither bites today. **Escalated rather than decided**, and it is the same question as row 14 from the creature's side: `item_rating` prices pieces and cadence, so a board of cheap components with six enchs on it rates like cheap components. Making rating count an ench would reverse *an ench does not move what an item is worth* — the rule that stops a Chonga'd blade being the best item in the game by the shop's reckoning — so this is not a bug to fix, it is a measure to choose. What makes it safe meanwhile is that she was **dressed by damage a second and not by rating**, which is what `PLAN-M16.md` §4.4 asks for and what the recon justifies. | worklist | **the human's** |
| 25 | **`data/enemies.json` went lossy the moment a creature carried an ench**, and `the_bestiary_file_reads_back` passed anyway because `EnemyData` simply had no such field. It has one now. The file's own doc says it is *the thing a person opens to ask what a creature wears*, and a creature whose teeth are six enchs and whose file does not mention them is a file that is wrong about the fight. | finding | done (M16.3) |
| 26 | **Health barely moves a fight at this depth and strength is the whole dial** — the Kettleworks finding a third time. Across a sweep of 5,200 to 8,800 health the outcome never changed; across 40 to 100 strength it went from a win at the buzzer to a loss at fourteen seconds. What health buys is how long she stands in sudden death. | finding | done (M16.3) |
| 27 | **A warp lands you on ground, and `warp_to` resolves no place on the landing tile.** So the Needle Room's first draft — a one-tile alcove with the lever *on* the tile you fall onto — was a lever nobody could ever pull: you land, nothing opens the card, and there is nowhere to step off and back on. Caught by `an_event_that_asks_something_still_reopens` looking for a walkable neighbour and finding none. **Fixed in the map, not the engine**: an alcove is two tiles, the one you land on and the one the lever is on, so a sinkhole behaves like every other warp in the game. `warp_to` is untouched. | finding | done (M16.3) |
| 28 | The general version of the row above: **`world::here` reports a gate and nothing else**, by design, and `warp_to` reports nothing at all. A third kind of arrival — *put down somewhere by something that is not a step and not a gate* — has no resolution path. It has never needed one and now it nearly did.  **M17 closed both halves.** `here` reports a beside-gate refusal now, because on a table you come to rest next to a gate; and the third kind of arrival has a name — `world::arrive_at`, written for a **landing**, which is the same shape: a tile you are on without having stepped onto it. There are four doors onto a tile and each answers a different question. `warp_to` still resolves nothing, and that is a decision rather than a gap now: `a_warp_resolves_no_place` pins it, and the doc says why — every warp lands you on ground, and a fall that opened a card would be a fall that started a conversation. | worklist | done (M18.4) |
| 29 | **A base class had no knobs, and the plan's two trees are built on tuning one.** `Effect::Tunes` was checked at parse time against `ExpertPower::knobs` and refused anything else — *only an expert class has knobs*. `ClassPower::knobs` and `::tune` are the widening, and `Character::class_defs` tunes a base power the way it tunes an expert's: **M13.6's thirty-eight dead nodes, one level down.** | divergence | done (M16.4) |
| 30 | **And widening it opened a collision `tunings_from` could not see.** `per_stack` is the Stoker's **and** the Patented Funnel's, so a character who was both would have had each tree's tuning applied to the other's power. That is precisely what the parse-time check was written to make impossible — *`carry` is Standing Fact's and Overwound Arm's* — arriving from the other side. `tunings_for(class, taken)` is scoped to the tree; `tunings_from` stays for the callers that mean all of them. Safe while there was one expert at a time and no base knobs, which is why nothing caught it before there were. | finding | done (M16.4) |
| 31 | **`ExpertPower::step` reads the knob's *name* and it needed to read the power.** A knob ending `_ms` is printed in whole seconds by the two experts that have one, so 500 is invisible — right for them and wrong for the Stoker, whose `describe` prints to a **tenth**: 800ms is 4.0s becoming 3.2s. `ClassPower::step` is the power's own answer and an expert defers to its own, unchanged. | finding | done (M16.4) |
| 32 | **The tree could not grant a pool or mind damage**, and the plan's trees want both. `Effect::Stat` read five fields and `Effect::StartWith` two, so `stat { nature: 20 }` and `start_with { insight: 20 }` would have parsed, cost points and changed nothing — the *eight skill nodes* failure, caught by `every_effect_key_is_one_the_engine_actually_reads` before it shipped. `Stat` grows `mind`; `StartWith` grows rage, faith, nature, insight and dread, which is what *what you are already holding when the bell goes* has always meant. | divergence | done (M16.4) |
| 33 | `Effect::line` collapses the pools into one *start every fight with* — spelling them out came to 127 characters on the Stoker's capstone, half again over what `a_mechanical_line_stays_short_enough_to_read_at_a_glance` allows. The same collapse `Node::line` makes for a row on every grid. | finding | done (M16.4) |
| 34 | **There is no mind axis on `Axis`**, which has fifteen and counts no maximum being eaten. The Whisperer's `requires` is `Arcana` and `Ward`, which is as close as the fifteen come. Inert today — `requires` is the fountain ranking and GM2D has no fountains — and it is the kind of thing that is wrong the day somebody wants one. **Measured and declined, with the reason written where it lives.** `ClassDef::requires` is read by *nothing* and `demand` is called by nothing: GM2D's fork is a screen at level five and the other two classes come off a counter. Adding a sixteenth axis to describe a class the engine never consults is a point spent on a promise that reaches nothing — the failure this repository has paid for four times. Kept for the reason `grown_health` is, and `Axis`'s own doc now says all of it so nobody mistakes it for live. | worklist | done (M18.3) |
| 35 | **`Effect::Stat { mind }` was dead the moment it was written**, and `every_point_in_an_expert_tree_buys_something` caught it: mind damage is read off `ItemProfile::stats.mind` and a character-level `Stats.mind` reaches no item, so a tree granting it granted nothing — the *eight skill nodes* failure exactly. It cannot be read off `player_stats` either, which sums every item's stats and would pay the board's own mind damage once per item. It travels through `Held::mind` now, which is the one door for *what the character contributes, once, beside the item* — the same door the tree's armour, its mana and its granted rules go through. | finding | done (M16.5) |
| 36 | **An expert's power is self-contained, and six of M16's would not have been.** A Fired Funnel's promise is *every stack the furnace buys is also mana*, and a furnace is the Stoker's — so without one the whole tree read dead. `light_the_furnace` gives all six their own; it is belt-and-braces (you cannot hold one without being a Stoker) and it is what lets the lint ask its question of the expert **alone**. Giving the fixture both parents instead was tried and buried five of the original ten under two parent powers' worth of noise. Told Once gets the same treatment for the Whisperer's threshold. | divergence | done (M16.5) |
| 37 | **Three tuning defaults were chosen so the knob never bound**, and the lint said so: Bare Furnace's `cap` at 6 was never reached by two bare frames' worth of stacks, Fired Funnel's `per_fight` at 6 was never spent, and Told Once's `per_curse`/`cap` were **raw health points** where the plan means percentage points of the maximum. All three retuned to bases the nodes actually move. | finding | done (M16.5) |
| 38 | **`purse_sweep` could not see a two-second move.** Flash Powder's window opens at ten and widens by two at a time; the sweep jumped 9,000 → 13,000, so twelve and ten were the same string. Eleven and fifteen added — the *compares zero with zero* failure with a stopwatch in it. | finding | done (M16.5) |
| 39 | **Cold Stoke's `standing` reads a curse that has stacked, not one that cannot expire.** §12.3 says *permanent*, and a permanent curse is Standing Fact's — a different expert — so a knob that only paid beside it would be a point you can only spend as somebody else. | divergence | done (M16.5) |
| 40 | **A board that deletes a maximum in one blow cannot watch a threshold move.** Told Once's board takes two of the Whisperer's nodes rather than all nine, because nine takes a foe from full to nothing in a tick and a line something steps over in one tick is a line nobody can watch. The general version: *a fixture strong enough to win instantly is a fixture that measures nothing.* | finding | done (M16.5) |
| 41 | **M16.4 and M16.5 are one commit, and the plan has them as two.** `every_pair_of_offered_classes_reaches_an_expert` is what makes them atomic: the moment `OFFERED` is seven, eleven pairs reach nobody, and there is no green tree between the roster growing and the table catching up. Committing the roster red and the table after it was the M16.0 pattern and was rejected here — two red lints across a commit nobody can bisect through is worse than one commit that says what it did. | divergence | done (M16.4/5) |
| 42 | **`make play` cannot walk a floor whose way on is behind a card.** Started on the Flat Below, the walker read one stake, did not answer it, and then walked into `(3, 11)` — a pocket behind a band of quicksand — for a thousand steps. M9.4 taught it that *a road refused three times is a road you stop walking at*; nothing has taught it that a road refused three times might be a road a **card** opens. Third instance of `PLAN.md` §6d row 3. The transcript is `testing/transcripts/m16-the-flat-below.txt` and it is kept for where it loops. **Taught, and the lesson is one line longer than M9.3's**: a road refused three times might be a road a **card** opens, so before barring it the walker goes and reads the nearest thing on this map nobody has read. Two things made it work and neither is obvious — the detour has to **outrank** the destination (a version that only ran when there was nothing else to do never ran at all, because the errand goes on pointing at the blocked tile, and it said *going to read (12, 13) first* six hundred times while standing still), and each card is tried **once** (a card the walker cannot reach either is the same loop in a different coat). Same run: 415 of 600 presses move against a thousand steps into quicksand, seven stakes pulled, and it gets off the floor. `testing/transcripts/m18-the-flat-below.txt`. | worklist | done (M18.5) |
| 43 | `testing/saves/at-the-reefs.json` is the block's walk fixture, in the repo's own `at-the-lip.json` pattern: the run, on the Flat Below, with a compass on the frame — **which §5.1 does not transcribe**, because a creature has no survey. Without one the run cannot open the way under the flat at all. | finding | done (M16.6) |
