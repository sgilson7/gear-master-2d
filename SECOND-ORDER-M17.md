# SECOND-ORDER-M17.md — the notebook

*The table's own notebook. Same convention as `SECOND-ORDER-M16.md`.*

---

## Rows

| # | row | kind | status |
|---|---|---|---|
| 1 | **`POWER_UNIT` is 30 and `PLAN-M17.md` §3 says 22.** The plan chose every constant on paper and says the recon has to check them; what the sweep found is a property the plan does not name — **distance has to be monotone in power**, because a cue where pulling back harder lands you *nearer* is a cue nobody can aim. At 22 it is not, and at every restitution below 80 it is not: a fast ball spends its extra speed on ricochets, and a bounce that costs too much makes a strong shot die at the wall it hit. **80 and 30 is the only pair in the sweep with no step backwards.** | divergence | done (M17.0) |
| 2 | **Seven hundred and twenty shots from the Treyway's start land on 164 of its 176 walkable tiles.** §10.3 asks whether 16×16 is too small to be an interesting table and says to report rather than fix; that is the number. **A table where one shot reaches ninety-three percent of the map is closer to a menu than to a course**, and it is the human's call. | finding | done (M17.0) |
| 3 | **The mean-distance measurement saturates by construction on a small map** — the maximum possible `|dx|+|dy|` is about fifteen — so *mean distance* flattens from power three whatever the constants are. The number that means something for a cue is **how many distinct tiles the shot can reach**, which is also what `aim_at` will need. Worth knowing before anybody retunes the table on the first number. | finding | done (M17.0) |
| 4 | `the_physics_has_no_floating_point_in_it` is a lint over the **source** rather than the behaviour, and it has to be: an `f32` that rounds differently in three engines produces a flight that is *nearly* right, which a hash cannot tell you about until somebody in another browser reports it. | finding | done (M17.0) |
| 5 | The angle table is a `const fn` over a nineteen-entry quarter turn rather than seventy-two typed pairs. `the_angle_table_is_a_circle` checks every entry is a unit vector and that opposite steps cancel — *a table typed by hand is a table that can be wrong in one entry*, and one wrong entry is one angle in seventy-two nobody would find. | finding | done (M17.0) |
| 6 | **Four of `PLAN-M17.md` §4.1's eight obstacle coordinates are on `A`** — range, impassable — and one shares the tideline's own tile. Same class as M16's floor drawings: the ASCII in the plan is a sketch and the rows are the map. The intent is kept and each moved to the nearest ground that carries it. | divergence | done (M17.1) |
| 7 | **A bumper at +30% against a wall at −20% is net-positive per round trip, so shots ran to `MAX_TICKS`.** Not a metaphor: a ricochet off the pass could outrun a full-power shot, which broke the monotonicity guard as well. A percentage compounds; a **fixed kick** settles at about `2.2k` whatever it started from. | divergence | done (M17.1) |
| 8 | **And a fixed kick still never stops**, because **friction is charged per tile crossed and a ball bouncing in place crosses none.** The fixed point is a ball that bounces for ever. So the pump is counted: `BUMPER_KICKS` is three, and after the third a bumper is a wall — which is what it already is to a ball approaching it. | divergence | done (M17.1) |
| 9 | **With obstacles on the table, mean distance stops discriminating the constants.** `POWER_UNIT` at 22 and at 30 both step backwards by at most a tile once eight obstacles are on the map, where on the bare map 22 stepped back and 30 did not. The constant is the bare table's answer; the shipped assertion is *never more than a tile*, which still catches a multi-tile reversal and does not claim a precision the table has not got. | finding | done (M17.1) |
| 10 | **The tide crossing stands on `tide`, which is impassable, so a ball cannot land on it.** §2.6 says a wall is not a place and such a gate is reached by stopping *beside* it — so the reachability lint asked *landable or landable beside*. **Both halves of that were wrong and row 16 is the correction.** | worklist | done (M17.3) |
| 11 | Everything on the Treyway is reachable in **two** rounds of shots, against §2.7's ceiling of six. Which is the same finding as row 2 from the other end: the table is small. | finding | done (M17.1) |
| 12 | **The beside-gate rule cannot return early.** The tide crossing's only landable neighbour is the tile the tideline's own card sits on, so a rule that reported the gate *instead of* the event made the way south unreachable on a table. `Step` has a field for each: you land on the tideline, read it, and the crossing is offered beside it. Landing **on** a gate overwrites it, which is the only ordering that cannot surprise anybody. | finding | done (M17.2) |
| 13 | **`world::arrive_at` is `step`'s own second half, extracted rather than written twice** — §2.3's *a landing is `Step`'s own resolution called at rest, do not write a second one*, as code. `step` calls it now too, so there is one answer to *what is here*. | finding | done (M17.2) |
| 14 | **`Step::nowhere` is `pub(crate)`**, because a pocket is a landing that did not land and needs to report *sunk* in the shape a refused step reports a wall. A second struct for it would be a second answer to *what happened when I tried to move*. | finding | done (M17.2) |
| 15 | **The recon §6 asks for, done**: `a_shot_counts_shots_and_not_tiles` is what keeps *nothing on a shot map reads `tiles-walked`* true rather than merely checked once. `shots-taken` is bumped beside it. | finding | done (M17.2) |
| 16 | **What a gate beside you offers is its refusal, never the way through** — and the first draft offered the way through, which walked a player over a bar still under nine feet of water. The cause is worth the row: **the tide crossing has never carried a condition of its own, because the impassable ground *was* the condition.** Every gate in this game is answered by the shim asking the bag; this one was answered by `walkable` refusing the step before anything asked the place. So a rule that reached it *without stepping* bypassed the only lock it had. Once the tenth cairn drains it the tile is `coast` and it is entered by landing on it like every other gate. Found by the browser gate in three engines, not by a unit test. | divergence | done (M17.3) |
| 17 | **And the reachability lint could not have found it, because it took "beside" for an answer.** A lint that accepts *landable or landable beside* cannot tell a gate you can enter from one you can only be turned away from — which is this project's *a lint that reads a list rather than the behaviour is the failure it exists to catch, one level up*, in a new coat. It asks the sharp question now: **can a ball come to rest on this tile, in some state of the world** — flooded **and** drained, because a map is not one grid and a drain turns `tide` into `coast`. | finding | done (M17.3) |
| 18 | **An obstacle is asked whether it can be *hit*, not whether it can be landed on.** Sharpening row 17 immediately failed on a bumper, correctly: a bumper is a tile no ball can ever come to rest on — that is what a bumper *is*. Exempting obstacles outright would have let an unreachable one through, so the lint reads `Flight.contacts` and asks the question that is true of them. | finding | done (M17.3) |
| 19 | **`world::here` needed the beside-rule too, and that is a soft-lock rather than a tidy-up.** On a table you come to rest beside a gate, so a player who lands next to the Reach's edge, builds a compass in the frame it opened and presses *Go in* was refused for ever. `gate_beside` is extracted and both doors onto a tile ask it — the same shape as `arrive_at` being `step`'s own second half. | finding | done (M17.3) |
| 20 | **A click on the canvas fired a full-power shot.** `pointerdown` set the cue from wherever the pointer was and `pointerup` fired it, so somebody clicking the map to focus it for the keyboard took a shot from the far side of the table. A shot needs a **drag** now, thresholded in CSS pixels because it is about the hand and not the map — and a press that never travels leaves a cue set with the keys alone. Found by a gate check clicking `#map` to focus, which is what every other check on this page has done since M1. | finding | done (M17.3) |
| 21 | **`preview_shot` dropped the contacts, so the arrow could not warn about a spike.** One builder for both payloads now (`contacts_json`), because *what is in the way* is one question — a page that drew a spike on the arrow and did not flash one on the flight would be two answers to it. | finding | done (M17.3) |
| 22 | **The trail is cleared where a shot starts, not where it lands.** A line wiped on landing is a shot you cannot look back at, and on a table looking back at the last one is how you take the next — the same argument the strip makes for keeping the last thing said. It also makes reduced motion mean what it says: *do not move things* is not *tell me less*, so the ball is at rest on the next frame with the path still drawn. | finding | done (M17.3) |
| 23 | **A shot can end on any screen, so a check that fires one must tidy any of them.** `dismiss_card` and `close_fight` cover two of six; a check whose `finally` covered two left a town standing over the page, the next check's first click timed out, and the whole failure list went unprinted — this file's oldest harness rule meeting a gesture whose outcome cannot be predicted. `clear_screens` is the one door. | finding | done (M17.3) |
| 24 | **`check_a_floor_still_steps` is the hardest check here to negative-test, and the reason is the finding.** Every break to *the arrows mean two things now* takes the whole gate down before the check runs — a floor that grows a cue is a dungeon nobody can walk out of. The break that isolates it draws a cue on a floor while still letting the key walk, which is the narrowest lie the page could tell. | finding | done (M17.3) |
| 25 | **A tile one step away is a tile you cannot shoot to**, and it bit three gate checks in a row. From directly adjacent every power overshoots or bounces off the wall behind, so a check that planted itself beside its target and called `cross` never arrived — the bar of shingle, the third town and the screen after it. That is not a fault: it is what a cue *is*, and the fix is a tee rather than a nudge. **Worth knowing before somebody puts two places next to each other on a table.** | finding | done (M17.5) |
| 26 | **The Undercountry is the second table and nothing about the country moved.** §4.2's three asks land as written — sand past each of the three ways up, the central road as the one fast lane, six obstacles with a bumper on each far slag range and the pocket in the corner with nothing else in it. Nine tiles gained a place and not one changed terrain. **A map becoming a table is a line in its header**, which is the return on `Traversal` living on the file rather than in the save. | finding | done (M17.5) |
| 27 | **The drifts go past the gates, not before them.** Sand stops a ball dead, so a drift on the approach is a wall in front of a door and one on the far side is a backstop. All three ways up could take the same treatment only because all three have open ground behind them — which is luck, and the next table's gates should be drawn knowing it. | finding | done (M17.5) |
| 28 | **`the-drove-way` was a place on two maps**, and `no_two_maps_name_a_place_the_same` caught it — the Treyway's new chute took a name the Reach already had. Renamed `the-drove-chute`. The lint is older than this block and is the reason a collision cost a minute rather than an afternoon of a warp landing on the wrong map. | finding | done (M17.5) |
| 29 | **`every_road_off_the_treyway_says_where_it_goes` enumerated what may stand on that map and nine new kinds arrived.** Its `other => panic!` arm is the good shape — *a new kind is a decision rather than a silence* — and what it wanted was one clause: an obstacle is hit rather than read, so what it owes is a **name** (the strip says what you hit) and reachability, which is a different lint's question. | finding | done (M17.5) |
| 30 | **`crates/lab/src/probe.rs` is `fan.rs` now**, which is `SECOND-ORDER-M16.md` row 7 closed: a scratch binary either earns a name or goes. It measures the thing row 3 says is the number that means something for a cue — **how many distinct tiles a shot can reach** — rather than mean distance, which saturates by construction on a map this size. It takes a map id, because there are two tables now. | finding | done (M18.0) |
| 31 | **And the second table answers §10.3 better than the first.** From each map's own start, 720 shots reach **161 of the Treyway's 176** walkable tiles (91%) and **247 of the Undercountry's 308** (80%). The bigger table is the one where a single shot does *not* reach nearly everywhere, which is the shape the question is about — and it is evidence for the answer being *draw them bigger* rather than *change the physics*. Still the human's call. | finding | done (M18.0) |
| 32 | **A detour that outranks nothing runs never.** Teaching the walker to read a card before barring a road looked like one condition and was two. The first draft ran the detour only when there was nothing else to do — and there always is: the errand goes on pointing at the blocked tile, so `want` is never `None` and the branch is dead. It printed *going to read (12, 13) first* six hundred times while standing still. **A detour has to beat the destination, because the destination is the thing that is blocked.** | finding | done (M18.5) |
| 33 | **And a detour that can spawn a detour ping-pongs.** Go and read A, be refused at A, go and read B, be refused at B, go and read A. Three separate guards were needed and only the third is the real one: not the tile you are standing on, not the tile that just refused you, and — the one that ends it — **only when you are not already on a detour**. A detour that is itself refused is a wall like any other and M9.3's rule takes over. | finding | done (M18.5) |
| 34 | **M17.4 put the refusal bookkeeping behind the wrong condition**, and only a walk found it: inserting the shot counter turned `if moved / elif refused` into `if moved / if table / elif refused`, so `stuck` was bumped on steps that *succeeded*. A walker that bars the tiles it just walked onto. Caught by reading the branch while fixing M16 row 42 — which is an argument for the notebook: the row that sent me back into that function is the reason the bug was seen. | finding | done (M18.5) |
| 35 | **`ClassDef::requires` and the whole of `Axis` are read by nothing.** Upstream ranked the classes a build qualified for at a fountain; GM2D's fork is a screen at level five and the other two classes come off a counter, so `demand` is called by nobody and fifteen axes score nothing. `SECOND-ORDER-M16.md` row 34 asked whether to add a sixteenth for the mind lane — **adding one would be a point spent on a promise that reaches nothing**, which is the failure this repository has paid for four times. Kept for the reason `grown_health` is, and the doc now says so where somebody would read it. | finding | done (M18.3) |
| 36 | **Four doors onto a tile, and now they are named.** `step` (you walked), `arrive_at` (you came to rest), `here` (you stood still and asked again), and `warp_to` (you were put down, and nothing resolves). M16 row 28 called the fourth *the third kind of arrival* and said it had no resolution path; M17 wrote one for a landing and it is the same shape. `warp_to` still resolves nothing **on purpose** and `a_warp_resolves_no_place` is what keeps that a decision. | finding | done (M18.4) |
| 37 | **The deploy gate caught a one-engine fault that passed standalone**, which is the argument for three engines in one sentence. `check_the_cue_draws_what_core_is_asked` drags the pointer, and `page.mouse.move` takes **viewport** coordinates — so after seventy-eight checks have scrolled the page, an end of the drag is off-screen, **webkit clamps it and chromium does not**, the pull lands under the threshold, and the cue is never drawn. Standalone, in either engine, it passes. The fix is the lesson this file already wrote for the playback controls: **measure, do not assume** — scroll the map into view and refuse the drag outright if either end is outside the window, with the numbers in the message. | finding | done (M18.8) |
| 38 | **A full walk from a new game crosses the Treyway in eight shots**, against `PLAN-M17.md` §2.7's ceiling of nine — and in **three** from a save planted on it. Both are inside the ceiling and the gap is the finding: a walker that arrives at the door with a destination on another map takes the long way round, which is the same *a walker with a destination stops being a player* this file has recorded since M11.9. 2,345 of 3,000 presses moved and six detours fired over the whole run, which is what the card-reading rule costs when it is not on a puzzle floor. `testing/transcripts/m18-a-full-walk.txt`. | finding | done (M18.5) |
| 39 | **The relink after a `data/` change is 127 seconds**, which is `SECOND-ORDER-M16.md` row 17 measured rather than estimated — 83 test binaries, and the run is a separate cost on top of it. A cold `--workspace` adds the lab and the shim as well, which is where the rest of the row's ten minutes goes. **`include_str!` is not the thing to change**, and the fix if one is ever wanted is fewer test binaries, which is a trade against one file per concern that nobody should make to save two minutes. | finding | done (M18.6) |
| 40 | **And `34f2dcd`'s own subject line claims more than was measured** — *the relink is two minutes, and the row said ten* — when what had been measured at the time was the **relink** alone, at 127 seconds, with the test run still timing. The two are separate costs and the row was about the total. The message stands as written because a message is a record of what was said; the number that is true is in row 39 and in `SECOND-ORDER-M16.md` row 17, and both say only what was measured. **A commit subject is a claim like any other, and this one was written between the measurement and the conclusion.** | finding | done (M18.6) |
| 41 | **And the run is 47 seconds, so the whole cost of a `data/` touch is about three minutes** — 127 relinking and 47 running, on an idle machine. `SECOND-ORDER-M16.md` row 17's *ten minutes* is a cold `--workspace`, which adds the lab and the shim on top of both. **Two attempts at this number were worthless and the tell was in the output**: one run read `real 1279.89` against `user 63.37`, which is twenty-one minutes of wall clock for a minute of work — it was queued behind three browser gates and a walker, not computing. *A timing taken on a busy machine is a measurement of the machine.* | finding | done (M18.6) |
| 42 | **A flight is twenty ticks and the page advanced eight a frame**, so the ball was drawn about three times and the whole shot was over in fifty milliseconds — with the full path painted before it had travelled any of it. Reported as *you should be able to watch the ball slide*, which is exactly what it was not doing. The clock drives it now rather than the frame counter, and the ball is drawn **between** ticks, which is what makes it slide rather than hop. The trail grows behind it and stays after it lands. | finding | done (M19.0) |
| 43 | **A diamond catches the ball**, reported as *it shouldnt have to perfectly land on it*. `PlaceKind::catches` is gates and bosses — the two things the map draws as a diamond and the two that are a way *into* somewhere — and they end the flight where they are hit. **Not towns and not events**: the Undercountry's town stands in the middle of its one fast lane, and a signpost you can roll past is a signpost. From the Treyway's start it takes the road west from 55 exact landings to **66 shots caught**. | finding | done (M19.1) |
| 44 | **The two mechanisms are for two cases and neither covers the other.** A diamond on ground the ball can enter is *caught*; one on ground it cannot — the tide crossing, on `tide` until the tenth cairn — is answered by the beside-rule, which hands back its refusal. The test asserts both by asking `walkable` rather than listing the exception. | finding | done (M19.1) |
| 45 | **Nine obstacles were drawn as events.** Reported as a question — *"there are a bunch more event diamonds, are they actual events or are they obstacles?"* — which is the question a player has to ask when five physical behaviours wear one mark. Only gates and bosses had a draw arm; everything else fell through to the generic small diamond. `look.rs`'s rule is that a mark is a shape nothing else draws, and it had been kept for everything on the map except the things the ball actually hits. | finding | done (M19.2) |
| 46 | **`Contact::Caught` had no arm in `contacts_json` and the preview was silent about it** — so the arrow could not warn that a shot was about to take you off the map. The remaining arms are named rather than `_` now, so the next contact kind is a compile error there instead of a thing the page never hears about. **That is the same `_` arm `Event::Burned` was still sitting in.** | finding | done (M19.1) |
| 47 | **The Kettle-Stoker dealt 746 and a classless character dealt 746.** Not close — identical, over one burn, on `common::geared_from`. Empowerment is `stacks × 5 × mana` and it scales a **magic** hit; the class's own doc explains at length that it buys empowerment with *somebody else's* pool, and never mentions that the thing it buys reads on one lane. `every_offered_class_reaches_something` passed because its fixture casts, which is *a fixture that can hold every branch is measuring a game nobody plays* said about a **lane**. | finding | done (M19.4) |
| 48 | **And the first fix took an expert's whole promise.** Making the furnace bank the mana it shovelled made the class work and left Fired Funnel — *every stack the furnace buys is also mana* — with nothing to promise. `every_point_in_an_expert_tree_buys_something` named `ff-twice-through` on the next run. The shipped fix touches only the Stoker's own stacks, which is the narrower change and the one that leaves the expert its own. **A fix that makes a thing work is not finished until you have asked what else it was load-bearing for.** | divergence | done (M19.4) |
| 49 | **`the_numbers_are_read_and_not_typed` was vacuous twice and only breaking it found either.** `contains("4")` passes with the fatigue figure hardcoded to *nine*, because the cart's forty-Fnorp fare has a 4 in it; whole-number matching over the whole glossary passes too, because a class promise says *every 4 seconds*. **A figure has to be checked in the entry that says it.** Third version, and the rule earned again: *break a new check and watch it fail before you keep it.* | finding | done (M19.6) |
| 50 | **A glossary's first draft listed the pool fields by hand and dropped rage** — the pool every player meets first. `Stats::parts` is the engine's own phrasing and is what the item card splits on and the standing panel draws; two answers to *what does a pool pay* is the shape this project keeps paying for, and it took about ninety seconds to reproduce here. | finding | done (M19.6) |
| 51 | **A check that prints what it found lets you notice it is wrong.** `check_the_furnace_shows_on_the_bar` went green while reporting *9 burns off None* — it read `burned[0].what` where an event's subject rides in `item`. The assertion was satisfied and the sentence was nonsense. A check that had only said `ok` would have hidden it. | finding | done (M19.8) |
| 52 | **The glossary is a proofreading surface, and it found four bugs on its first read.** It is the first screen in this game that prints all twenty-eight promises together — *"you may hold 1 enchs a component"* and *"1 stacks of mana empowerment"* are the kind of thing nobody notices in a match arm and everybody notices in a column. `no_promise_is_ungrammatical_about_a_count` is over **every** promise rather than the three that happened to be wrong, and **it found the fourth the moment it existed**. That is the argument for writing the general check rather than fixing the instances, made in about ninety seconds. | finding | done (M19.3) |
| 53 | **The glossary called one class two names**: an expert's aside said *what Berserker and Stoker reach together* under an entry titled **Gorillathon**. `Theme::retell` swaps whole words and does not cover the class table, so the pair travels beside the sentence on `Entry::pair` and the shim puts the player's word in — core writes the sentence, the shim translates the names in it. Found by *looking at it*, which is what this repository's art rule says about anything drawn, and a screen is drawn. | finding | done (M19.2) |
| 54 | **Three expert promises read oddly at their untuned values** — *"a cast refunds 0% of what it cost"*, *"keeps 0 of its turns"* — because the knob is zero until points are spent in its tree. Accurate for a character who has just taken it, and it reads as broken. **Left as it is**: `describe` is shared with the fork card and the tree tab, where naming a knob at zero is how you know the knob exists. Worth a decision rather than a silent change, and it is the human's. | finding | **the human's** |
| 55 | **`shoot` never printed a place's refusal, and `walk` has since M15.** Reported as *"make sure you can get through the bottom of the first overworld map, its still pink for me"* — you come to rest beside the bar of shingle, `arrive_at` fills in `blocked` and `refused_by` with the whole sentence including what opens it, and the page threw it away. **The third instance of *the land is pink and it says no way through***, through a door that did not exist when the first two were fixed. A payload field nothing reads is the same failure as a number nothing prints. | finding | done (M19.4) |
| 56 | **And the way through works — one shot from the map's own start.** Once the tenth cairn is cut the bar is `coast`, `aim_at` finds a shot at it from the start tile, and it crosses. The blocker was never the crossing; it was silence. | finding | done (M19.4) |
| 57 | **Events are reached by stopping on one, and nothing helped you aim.** Reported as *"how are events on the overworld accessed? it seems i cannot access them right now"*. The mechanism was never broken — **686 of the Treyway's 720-shot fan land on the Kettleworks road, from 159 tiles** — and an event is one tile that deliberately does not catch the ball. So the cue draws the road ahead now: the whole predicted path, the tile it stops on, and a box round anything it hits. `preview_shot` is the same `shot::shoot` the fire button runs, so what is drawn is what happens. **A traversal mechanic you cannot aim is a dice roll**, and that was the complaint under the question. | finding | done (M19.5) |
| 58 | **Haloed, not tinted**, for the reason the armour label is: the ground under a predicted path is anything from pale sand to dark slag and no single ink reads on both. A dark stroke with a pale dashed one over it reads on every terrain this game draws. Third time that trick has been the answer. | finding | done (M19.5) |
| 59 | **The negative test for row 55 was wrong twice, and both were the check reading a different subject.** First it looked for *"tenth"* — which the tideline card standing on that very tile also says, so it was matching the event's prose. Then it used `cross`, which falls back to `window.__here()` and so goes through **`walk`**, the door that already worked. Both were green with the fix deleted. It asserts on the half only the engine appends (`Requirement::wants` naming the errand and the gate), fired by a shot and nothing else. **Two vacuous checks in one sitting, and neither would have been found by reading them.** | finding | done (M19.5) |
| 60 | **Two runner scripts wrote the same done-file, and a suite count came back 144 too high.** `/tmp/gmcore.sh` (the engine) and `/tmp/gmtest.sh` (the workspace) both `echo done > /tmp/gm-done`, so a watcher waiting on that file reported whichever finished — and **1,137** was published as the engine suite's figure when the engine suite is **993**. Caught by arithmetic rather than by the tooling: M18 was 976 and M19 adds about fourteen tests, so 993 is the number that adds up and 1,137 is not. *A second copy of a constant goes stale in three engines at once*, said about a **filename**. | finding | done (M19.5) |
