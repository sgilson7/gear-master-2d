# SECOND-ORDER-M20 — the notebook

Written as it goes. The last milestone reads it as a worklist.

| # | row | state |
|---|---|---|
| 1 | **The glass barely bites.** Seven cells takes all 28 pairs and eight takes 55 of 56 triples, so the only arrangement the shape actually refuses is three three-cell ingredients. The lint catches a glass that carries *nothing*; it does not catch one that carries almost nothing. Either the ingredients want to be bigger or the Apothecary's first node wants to be the one that makes the glass generous rather than tight. | open |
| 2 | `gate_toward` was deleted when `unlock.rs` took over its one caller. Nothing else read it. | closed |
| 3 | **Three tests moved because a quest reward moved a fixture.** `common::geared_from` takes every errand's pay, so the three Wextreen Sands errands made the deep less deadly and two Stack floors a walk. They pay money instead. The general form is worth keeping: *a fixture that is the yardstick for how dangerous a place is must not be wearing what that place paid.* | closed |
| 5 | **A specialization with no `requires` became the fountain's fallback.** Two inherited tests hold *exactly one class asks for nothing*, and it is the floor class. Nothing in GM2D ranks at a fountain, so the numbers are a shape rather than a gate — but the invariant is worth keeping and the fix is two axes. | closed |
| 6 | **`openLog` painted before showing**, which was harmless while the log was a flat list and is the skill tree's two-milestone bug the moment the rows have wires between them. Swapped before it could ship. | closed |
| 7 | The Cairnworks' four bosses all wear The Unwritten's two slots. Their *healths* and *strengths* are written rather than measured — `what a creature deals is mostly how many items its board makes` says the lane gates will hold and the durations will not. Wants a measurement against `common::geared_from` before anybody calls them tuned. | open |
| 8 | **The yardstick moved twice, in opposite directions.** Adding the Cairnworks drops made `geared_from` stronger (two Stack floors became a walk, and the furnace check went quiet); fixing it by giving drops the way `pay_a_win` gives them — not if you already hold one — made it weaker again, and the four floors I had just tuned against the inflated board all had to be re-measured. **Tune content against a fixture only after the fixture has stopped moving.** | closed |
| 9 | `data::MAPS.len()` is asserted by name in `undercountry.rs`, which is the right shape and means every map block edits it. 25 → 29. | closed |
| 10 | **`.mapwrap { line-height: 0 }` gave the area card zero height.** It is set so the canvas gets no baseline gap under it, and every other child of that box inherits it — so the card was in the document, unhidden, 638 pixels wide and **nought high**. Only a browser measuring it said so. Same family as the `.card` and `.tabs` collisions: *a property set on a container for one child is a property every other child gets.* | closed |
| 11 | **Three of the four new browser checks were wrong before the code was.** One read `window.__log()` as an array when it is `{pinned, errands}`; one gave a single-tile landing six shots when *a tile one step away is a tile you cannot shoot to*; one walked a fixed number of steps and never left its own region, so it asked nothing. The one real bug they found was row 10. | closed |
| 4 | `Held` grew a `stats` field, which is the first **rate** it has ever carried. Everything else in it is a quantity. Worth watching that the next thing wanting a one-fight rate uses it rather than inventing a second door. | open |
