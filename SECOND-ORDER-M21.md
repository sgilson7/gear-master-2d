# SECOND-ORDER-M21 — the notebook

Written as it goes. `M21.12` onward are read off this file: every row marked
`open` is work somebody has to do or a finding somebody has to write down, and
the last milestones of this block are whatever closes them.

Format is `SECOND-ORDER-M20.md`'s — numbered rows, a bold first sentence, and
`open` or `closed`.

| # | row | state |
|---|---|---|
| 1 | **`Game::eq` had never learned about M20's bags.** That operator is hand-written and lists fields by name, and the comment beside `banked` says exactly why that matters — *a save that dropped it would round-trip green*. The larder, the glass, the pack, what you have drunk and what you are were all added in M20 and **none of them was added to `eq`**, so `tests/save.rs` could not have caught a loader that lost somebody's ingredients. Closed by adding all five beside the Plot's drawer, which is the sixth. The general form is worth keeping: **a new bag is three edits, not two — the struct, the save, and the comparison.** | closed |
| 2 | **The drawer went on `Character` and the plan said `WorldState`.** Every other bag in the game is on the character — `owned`, `banked`, `larder`, `potions`, `retort` — and `WorldState` holds what has *happened*: what is answered, what has drained, what a shop has sold. A drawer in the world would be the only bag in the game not on the person holding it. Divergence, recorded in `CLAUDE.md`'s table. | closed |
| 3 | **Three of the twenty-four seed-and-bed pairs fit nowhere without a turn.** Measured before the beds were authored, which is what the recon step is for: `toad-ichor` and `tallow-drip` fit no anchor in Kettleworks' gapped row and `reef-salt` fits none in the pit's round bed. `PLAN-M21.md` §M21.1 gives `Game::plant(town, seed, at)` with no turn. A crop turns now — `Crop { seed, at, stage, turn }`, which is `brew::Seat`'s own shape, because the retort has tried every rotation since M20 and *a heuristic that refuses an arrangement somebody can see with their eyes* is the thing `brew::fits` exists not to be. Divergence recorded. | closed |
| 4 | **`git checkout <file>` in the middle of a negative test threw away uncommitted work.** Backing a file up with `cp` before breaking it is the habit; on the fifth break I reached for `git checkout` instead, and `plot.rs` went back to its M21.0 commit — losing the turn on a crop, the harvest yield, the clock and both refusal helpers. Rebuilt from the conversation, no loss beyond the time. The rule is narrow and worth keeping: **while a file has uncommitted work in it, the only safe undo is the copy you took yourself.** | closed |
| 5 | **A paint that refreshes and a refresh that paints is a stack overflow**, and it does not announce itself. `Board.refresh()` ends by calling `onchange`, so wiring `onchange = () => paintBed()` while `paintBed` calls `refresh()` recurses — and what it looked like from outside was **the town screen never opening**, with no console error and the bed's own sentence painted correctly. The bench has split *open* from *paint* since M20 and the reason was not written down; it is now. Worth watching for the Kennel's yard and the Stall's shelf, which are the same `Board` and the same two functions. | closed |
| 6 | **One cell took the pit's bed from 11 of 28 pairs to 25.** Measured before the companion table was authored. The pit's round-with-a-stone shape — not its size — blocked seventeen of the twenty-eight pairs from ever touching edge-on, while Kettleworks at the *same* eleven cells reached twenty-five. The pit is the only town for the first hour, so the Plot's whole second door would have been shut where every new player is. Squaring one corner fixes it. The general form: **a bench whose shape is the puzzle wants its pair table measured, not its cell count.** | closed |
| 7 | **Four of M21.6's six "new" ench effect kinds already exist in the engine.** *Before adding a system, grep for it* — the rule this project wrote after `explain.rs` was built with a duplicate describer already in `piece.rs`, and after M8 opened with a request to add curses to a game that had had fifty-nine cursing components since the fork. The table: `the-bramble-coat`'s *returns* is **`Stats::reflect`**, documented as *"percent of what your armour absorbs that is turned back on whoever swung"* and read in `combat.rs`; `the-first-word`'s *ambush* is **`RunningItem::overtakes`**, *"the first firing of the fight runs twice"*, with `has_fired` beside it; `the-one-page`'s *once at five times* is **`Effect::Fragile { pct }`**, which is an ench kind already and is what the Chonga Swing is; `the-slow-match`'s *coats* is **`Rule::CurseOnActivate`**, granted by a tree since M8.3. Only `the-tithe-ring` (leech to **mana**, where `leech` returns health) and `the-long-count` (a per-activation ramp) are new. M21.6 is therefore mostly *exposing existing mechanics as enchs for the first time*, which is a much smaller and more honest milestone — and the six games are still cited, because the ench is new even where the mechanic is not. | open |
| 8 | **M21.2 pays ench seeds for enchs M21.6 defines, and the M21.5 deploy falls between them.** A seed that grows into an ench nothing defines is content reachable from nowhere — the Apothecary's own bug, one system along. The two milestones are swapped: the enchs are built first, then the pairs that pay them. The plan's own build-order constraint is *Plot → Kennel → Stall* and this respects it. | closed |
| 9 | **Adjacency is often forced rather than chosen.** Measured over all three beds: at Kettleworks only **10 of 28** pairs can be planted *apart* at all, at the third town 15 and at the pit 17 — so for most pairs, two crops that both fit must touch. `SYSTEMS-PITCH.md` calls companion planting "the puzzle", and a puzzle you cannot fail is not one. The defensible reading is that the decision is *which two seeds*, not *whether they touch*, and the geometry decides how many fit — which is still twenty-eight choices. The Grower's `bed_cells +3` opens it up. Worth a number in the glossary and worth the human's eye before anybody calls the bed tuned. | open |
| 10 | **A percentage on a yield of two rounds to nothing**, and the Grower's whole promise with it. `yield_pct` is 120 untuned and `2 * 120 / 100` is two, so the specialization paid exactly what everybody else got — the test read *a grower pulled 2 and everybody else pulls 2*. This is the `SPELL_MANA_COST` failure this project already has written down — *a percentage off three rounds to nothing*, found in `cast_price` in M13 — with a crop in it instead of a cast. `HARVEST_YIELD` is four now, which gives the ladder room (120% is five, a finished tree's 170% is seven), and the sum is rounded the payer's way. **The general form is worth saying once more: a knob that is a percentage needs a base big enough for one point of it to be visible.** | closed |
| 11 | **The plan's M21.3 deliverables *the paper* and *the fork* are superseded.** `PLAN-M21.md` was written against `40e3121`, before specializations had any way to be taken at all — the Apothecary shipped unreachable and the fix, built since, is **a trainer**: one person on one map who takes you on. So the Grower gets a trainer at the bottom of the Silt Stair rather than a fourth paper on Spike's counter, and there is no specialization fork to put a second card in. `every_specialization_has_a_trainer` covers all three. | closed |
| 12 | **There is no player-side slot for an ally, and M21.5's plan costs far more than it budgeted.** `simulate_party` takes `specs: &[MonsterSpec]` — that slice is the **enemy** party. The player side is a single `&mut Combatant` threaded through the fight loop: **101 `pick(p, foes, …)` call sites, 9 functions taking `p: &mut Combatant`, 16 `Side::Player` matches, in an 11,482-line file** whose own rules say *inherited on purpose — do not simplify*. Making the player side a slice also rewrites the golden combat fixture, which is a character-for-character comparison against upstream. The plan budgeted "one combat change"; the change it names is the largest refactor available in this repository. Measured before anything was written, which is what the recon step is for. | open |
| 13 | **A creature out is fed by its own kin, for free.** A win pays a family-matched ingredient into the larder *before* the feed runs, so fighting anything in the creature's own diet bucket feeds it out of the corpse — and the eight buckets cover twenty families between them, so "anything in its bucket" is most of what an early player meets. `FEED_EVERY = 1` therefore costs nearly nothing in practice: the larder only empties if you deliberately fight out of the bucket. Found by writing the test, which is the good way to find it. Whether the Kennel should cost more than that is the human's — the cheap fixes are feeding *before* the drop, or a diet that is not the creature's own family. **Closed by M21.13**: the ingredient drop is a roll at 250 per mille, so a win pays a quarter of one and a creature out eats one a fight — fighting cannot feed it and growing must. | closed |
| 14 | **The M21.3 deploy failed in CI on a click the local gate passed**, and the cause is the harness rather than the page: `page.click("#download")` is the *first* click in a dozen checks, so a screen left up by the check before is a thirty-second timeout **here** rather than a finding **there**. Adding the bed and the run made the town screen two panels taller, chromium lost a race webkit won — *webkit walked the gate* is in the same log — and the run died. `CLAUDE.md` already records this shape from M20 and the fix that time was one `clear_screens` in one check; it has now cost a deploy twice, so the clearing is **the door** rather than a guard somebody remembers to write: `download_save(page)`, fifty-eight call sites through one function. | closed |
| 15 | **The first creature anybody kennels contributes nothing** — because only the Cave Rat has innate attacks, and every other creature in the ladder fights purely out of its gear. A companion built from gear therefore gave the rat *zero items*, and the rat is what you beat six times long before anything else. That is *a thing that works and cannot be seen*, on the one creature every player starts with. Fixed by contributing its teeth as well: a bite is a profile with one trigger, which is what `RunningItem::from_attack` already turns one into — so `ItemProfile::bare` is the same thing one layer up, for the one caller that wants a profile rather than a running item. Found by a test asserting *a creature out contributed nothing*, which is the assertion doing its job. | closed |
| 16 | **The feed clock read a counter nothing in `settle` bumps.** `world.count("encounters")` is the *shim's* — bumped when a step rolls an encounter, never by `fight::settle` — so it was zero in every test and in every fight reached any other way, `0 % feed_every == 0` held always, and a creature ate every fight however the Handler was tuned. I chose it on the principle that *a second counter meaning nearly the same thing is how two answers get made*, which is the right principle applied to the wrong counter: **fights since it last ate** is not a thing anything else in this game counts, so counting it on the creature is not a duplicate. `Kennelled::since_fed`. Found by a test asserting it stayed out, which watched it go in. | closed |
| 17 | **The kennel shipped with no way into it, in the block that opened by fixing exactly that.** `Game::kennel_offer` and `Game::take_along` landed in M21.4 with seven tests between them and **no caller anywhere in the shim or the page**, so the run, the yard, the feed and the twenty-eight diet pairs all went live at `77bb4b19` with nothing able to enter the kennel. That is the Apothecary's own failure — and I wrote the Apothecary's fix three days of work earlier. A rule with tests is not a rule a player can reach, and the tests are what made it look finished. Reported by the human, which is the part that stings. Closed by M21.12, and by `every_way_into_the_kennel_is_reachable` beside the browser check — *is there a rule* and *can a player reach it* are two questions and they want two tests. | closed |
| 18 | **`fight::settle` clears the encounter, so the screen after a fight does not know what it beat.** Right, and it meant the first draft of the offer asked `g.encounter` on the result screen, got `None` every time, and offered nothing on any fight at all — a button that is correct and never appears. The creature travels with the settlement now (`"beaten"`), which is the same shape as *a derived number needs somewhere it is shown*: what the next screen needs has to be handed to it, not left where it was. | closed |
| 19 | **`close_fight` could not close a fight that had reached its result.** It clicks `#run` — *Walk away* — and the action bar swaps its contents per stage: the result stage has only the advance button. So any check that ran a fight to its end hung for thirty seconds, which is every check that reads a receipt. It tries whichever of the two is there now. Found by the first check that did read one. | closed |
| 20 | **Making the ingredient rare closed the Kennel's feed row for free**, which is worth noticing because the two were asked for separately. Row 13 was *a creature out is fed by its own kin*, and the answers I was weighing were feeding before the drop or giving a creature a diet that is not its family — both changes to the **Kennel**. The human's *"ingredients should be much rarer"* is a change to the **Plot**, and it fixes the feed as a side effect. **Two systems sharing one resource means a rate set for one of them tunes the other**, which is the argument for the larder being one bag rather than two. | closed |
| 21 | **A test had the old certainty baked into its arithmetic.** `a_creature_out_eats_at_the_bell` asserted `before + 1 - 1` — the win pays one, the creature eats one — which was exactly true while the drop was certain and became nonsense the moment it was a roll. It stocks the larder itself now and fights out of the bucket, so the only thing that can move the count is the eating. **A test that encodes a rate has to be rewritten when the rate moves; a test that encodes the behaviour does not.** | closed |
| 22 | **A counter three cells tall refuses the weapon every character starts with.** The Stall's first shelf mask was nine cells in a four-by-three, chosen because the retort's seven read well. The Iron Blade is **one by four**, so it fit nowhere at all and `shelved_is_not_carried` failed on its first run with *"The counter stops short: (0, 2), (0, 3)."* Measured the catalogue rather than nudging the mask: bounding boxes run 1×1 to 6×6, **19 components are 4×1**, 537 of 568 are inside 3×2, four are 4×3 and two are longer than five. The shelf is fourteen cells in a five-by-four now — it takes a 4×3 exactly and has two cells left, which is the packing decision, and it refuses six of the 568 by name. **The general form is the retort's rule from the other side**: *a glass where every triple tiles carries nothing* was a shape too generous; this was a shape too mean, and only measuring what has to go in it tells you which you have. | closed |
| 23 | **A load-time check and a lint asking one question makes the lint look vacuous.** `StallData::parse` refuses a bargain whose ench has a price, and so does `a_bargain_is_never_on_the_barrel` — so negative-testing the lint by pricing one **panicked in `data.rs`** before the assertion ran. That is not the lint being wrong: it also asks the half `parse` cannot, which is whether a `Bargain::Piece` is on a town shelf, and `parse` has no business reading `shops.json`. But it is *two lints asking one question* one step along, and the honest form is **a check whose negative test cannot be made to fail through the check is a check nobody has proved.** Worth deciding which of the two owns the ench half. | open |
| 24 | **Every bench in this block has needed `paint` split from `paintText`.** Four times: the bench in M20, the bed — which found it as a stack overflow that looked like *the town screen never opening*, with no console error — the run, written split because of it, and now the Stall. `Board.refresh()` ends by calling `onchange`, so an `onchange` that paints and a paint that refreshes is a loop that announces nothing. **Four instances is a convention rather than a hazard**, and it belongs in `CLAUDE.md` beside *a page that draws a world has to be told which world*: something every caller has to do that nothing enforces. | closed |
| 25 | **`Board#onpoint` hands back a key and not the thing pointed at.** The Stall's first price control read it and got a *string* — `bag:12`, or an item's joined piece ids — where it wanted the payload, which is `onpiece`'s. Both are right for what they were written for: a key is what a highlight needs and an object is what a card needs. What it produced was a price box that was always empty. It is a list of price controls beside the grid now rather than a box that follows the pointer, which is the better screen anyway: **a number typed into a canvas is a control a keyboard cannot reach**, and a box that moves while you are using it is a box you cannot use. | closed |
| 26 | **Four benches shipped and not one errand is about any of them.** The retort, the bed, the run and the counter are four systems a player can reach and the quest log — *the one screen in this game that says something has opened and it is somewhere else* — has never once pointed at one. That is M21.4's own failure a level up, and the human found it rather than a lint: asked for as *a quest chain for each new system, that the quests are predicated upon using the system*. **M21.16.** | closed |
| 27 | **None of the four `Goal` kinds can ask about a bench.** `Bring` is answered by a component in the bag and a potion is not a component; `Slay` is answered by a creature dying and a companion is a creature that did not; `Word` is answered by standing somewhere; `Clear` reads a boss's tile id. That is the same gap `Clear` was written to fill in M20 and the same reasoning — *a boss tile is the one thing there is exactly one of* — so the answer is **one new arm with five variants** rather than four arms asking four nearly-identical questions, which is how two lints drift into two answers. | closed |
| 28 | **The counter would have sold an errand's tally, and `make play` is what found it.** The walker put a Bengulon Toad Eye out at twenty-five Fnorp — because a toad eye is the cheapest thing in an early bag and the walker sells the cheapest thing. Selling one breaks the errand **silently**: the tally is gone, the hand-in never comes, and nothing anywhere says why. `can_equip` has refused `PieceKind::Quest` since M8 with *that is a quest item — it is carried, not worn*, and **a counter is the fourth consumer that has to know** (the board, the bench, the bank, the counter). The general form is the one this file keeps writing down from the other side: *shelved is not carried* buys you four consumers for free, and the fifth is the one you have to tell. | closed |
| 29 | **`make play` found in one run what fourteen tests did not.** All fourteen `tests/stall.rs` cases plant exactly what they mean to sell; the walker plants nothing and sells whatever a real bag holds, which on the first afternoon is two starting components and four toad eyes. That is the M8.8 Auto-pack finding again — *`make test-ui` walks a route chosen to exercise checks and `make play` starts a new game and plays it* — and it is the argument for the transcript being a deliverable rather than a formality. | closed |
| 30 | **The street broke every gate check that reached into a town panel.** Seven buildings with one visible at a time means `#quests`, `#shelf`, `#barrel`, `#bank-bag` and `#stall-box` are `hidden` most of the time, and Playwright refuses to click what it cannot see — the gate died at check 36 on a thirty-second timeout. The fix is one door, `enter(page, sel)`, and it is **derived from the DOM**: it asks the selector which `.building` it is inside and presses that tab. A map of id → building written in the harness would be a second copy of the markup and would go stale the first time a panel moved, which is the seventh hand-written list this project would have paid for. **Reading is unaffected** — `querySelectorAll` sees a hidden panel perfectly happily, which is why only the seven interacting sites needed it and is also *a check that counts elements is not asking whether they are drawn*, from the other side. | closed |
| 31 | **Which building a town opens on is kept across visits, and that made the street's own check pass wrongly at first.** It presses a tab and watches the panel change; pressing *the last* tab is a no-op whenever the last tab is the one already showing, which it was, because the check before it had left the counter open. It picks a tab that is not the open one now. The keeping is right — a player working a bed wants the bed next time, the same reason the bank's sort order is kept — and **the check has to be written against state that persists rather than against a fresh screen.** | closed |
| 32 | **The street's check cannot be negative-tested through the check.** Gutting the tab's `onclick` does not fail it: it takes the whole gate down thirty checks earlier, because every other town check now reaches its panel through that click. Two of its three assertions break cleanly — *seven buildings are laid out at once* and *the street has 0 buildings on it* — and the third cannot be isolated. Same shape as `check_a_floor_still_steps`, whose note says every lie about *the arrows mean two things now* takes the gate down before the check runs, and the same shape as row 23 one screen along. Worth knowing that **a check wired into the way every other check moves cannot be broken in isolation**, and that this is a property of the screen rather than of the check. | closed |
| 33 | **Every specialization's tree drew and refused every node, on a live build.** Reported from play: *got the specialization from the guy in the wextreen reach dungeon, and the skill tree I got was the apothecary but whenever I try and put points into it, I get a message saying "that is Apothecary's, and you are not one".* `Character::spendable_trees` is `classes()` **plus the specialization**, and `all_trees_json` has drawn the tabs off it since M21.3 — `take_skill` was still passing `classes()`. **A rule with two answers**, and the tell is the one this file keeps recording: the two did not disagree about *the rule*, they disagreed about which list **is** the rule. Broken by putting `classes()` back, the lint names all four specializations, not the one reported. | closed |
| 34 | **The lint for it had to be the general property, not the reported case.** *An Apothecary can spend in the Apothecary tree* is a list of one and would have passed a build where the Chef could not. `every_tree_a_character_is_offered_can_be_spent_in` walks every specialization **and every offered class**, takes the roots of each tree it is offered, and fails on `WrongClass` or `NoClassYet` — *a lint that reads a list rather than the behaviour is the failure it exists to catch*, and the behaviour here is `take_skill` itself. | closed |
| 35 | **The Whisperling's whole lane was invisible, and it is the `Event::Burned` bug a third time.** Reported from play: *I have chosen the whisperling class and insight, dread are not explained anywhere or shown as stacks in battle.* Two causes and both are lists: the fight bar's pool row was `[mana, rage, faith, nature]` behind a hand-written `pool_index` answering `None` to everything else, and `Event::Dreading` sat in `fight_json`'s `_ => ("other")` arm — **exactly where `Burned` was until M19 and `Cursed`, `Warded` and `Stunned` were until M8.2.** The row is `Resource::SPENDABLE` plus insight plus dread now, derived, and the index comes off the same list. **Seventh hand-written list, sixth *a derived number needs somewhere it is shown*, and the second time it has been a whole class.** | closed |
| 36 | **The glossary was the only screen that mentioned insight, and it mentioned it to say it pays nothing.** The pools entry is built from `pools_worth_holding`, which is exactly the pools that pay a *wearer* for sitting on them — so dropping insight and dread from that list is right, and being the only line about them is not. It carries the mind lane's own sum now, off `DREAD_DIVISOR`. **A list that is correct about what it is for can still be the only thing anybody reads**, which is a different failure from a wrong list and wants a different fix: another entry, not a wider list. | closed |
| 37 | **A check hardcoded `length === 4` for the pool row**, so widening it to six failed `check_both_boards_tick`'s *the replay is not carrying armour and pools*. It reads `log.pools.length` now. Same shape as the barrel gate's own hardcoded `12` for `BARREL_CEILING`: **a gate that hardcodes a number core owns is a second rulebook with a longer feedback loop than the first**, and this is the second time in this repository. | closed |
| 38 | **The mind-lane check had to count columns rather than match words**, because a pool's *name* is the world's and goes through the theme: insight reads as *mansus-sight* and dread as *anticipation*. Looking for the engine's words in a themed payload is the harness asking the wrong register — TONE 13a from the outside. Six columns is the assertion and core owns which six. | closed |
| 39 | **The street broke `make play` the same way it broke the gate**, and the fix is the same three lines in a second file. `#tins` is in the chandler and the walker clicked it blind. `enter(page, sel)` is derived from the DOM in both, which is why writing it twice is not two answers: neither carries a map of id to building, both ask the markup. **A screen change that hides panels costs every tool that drives the page**, and the cheapest version of that cost is one function per tool rather than a list per tool. | closed |
| 40 | **The walker kept its own copy of what may be sold, and was wrong about the word.** `x.kind !== 'Quest'` — the payload says *"quest item"* — so it put a toad eye out four times a visit and read the refusal four times. Both the walker and the gate try each thing in the bag cheapest-first now and let `Game::shelve`'s refusal be the filter. **The engine's refusal is the filter**, which is the same rule as *the page draws numbers core sent it* applied to a predicate. | closed |
| 41 | **`make play` has a pre-existing timeout at the Reach's instrument screen** — `#instrument-board` intercepts the click the walker is trying to land — and it is in the M21.8 transcript as well as this one, so the street did not cause it. Recorded rather than fixed: the walk reaches level 11 and the whole of the Stall before it, which is what the milestone needed from it. Worth a milestone of its own if the walker is to reach the deep maps again. | open |
| 42 | **The browser check for the specialization bug belongs in the check that already makes you one.** The first draft was a throwaway probe against the live page and it could not even plant a save — a bespoke harness reproducing the gate badly. `check_a_trainer_takes_you_on` already walks onto a trainer, takes the training and reads the sheet; **spending a point in the tree it hands you is the next sentence of the same story**, and with the fix reverted it reports the reporter's own words: *"that is Apothecary's, and you are not one"*. The rule: a check about a thing is an arm of the check that already reaches that thing, not a second walk to the same tile. | closed |
| 43 | **The check read `skills_taken` and the payload calls it `taken`.** `?? 0` turned the wrong key into an empty list twice, so `0 != 1` and a working build failed — **a check lying in the direction that looks like a bug**, which is worse than one lying the other way because it sends somebody to read the engine. Third time in this block a payload's rows have been read under the wrong key, after `.quests` for `errands` and `window.__log()` twice. **A `??` default on a key you have not verified turns *you asked the wrong question* into *nothing is there*.** | closed |
| 44 | **The Stall's gate check drove the payload and never clicked**, so it was green on a build where nothing could be put on the counter at all — **two faults at once**, and a player found both. The payload's `id` is a number and `stall_legal_anchors` took a `String`, so wasm-bindgen threw on the first pick-up; and the bag was drawn on the canvas, which an eighty-eight-component run made two thousand pixels tall, putting it behind the town's own pinned action bar. The rule: **a check that drives the payload asks the rulebook; a check that clicks asks the page.** Every other board check in this repository clicks, which is why none of them has shipped this. | closed |
| 45 | **A run's whole bag is not a thing to draw on a board.** The packing screen's bag is a handful and the counter's is everything loose you own. It is an HTML shelf now, ordered the way the bank's is, and `Board#hold` is how an outside control puts something in hand — the board still owns *where it may go*, which is the one question it exists to be the only answer to. | closed |
| 46 | **`replaceChildren` on a strip you have moved live nodes into destroys them.** The four door buttons were moved into `#town-street`; the next `paintStreet` wiped the strip, and from then on `document.getElementById('pack')` was `null` — **with no error anywhere**, which is how it reached the gate as a click timeout thirty checks later. They are held by reference now. **A node you moved is a node you have to keep hold of**, and the general form is the one `.card` and `.tabs` already taught: a container you share is a container that can take your things. | closed |
| 47 | **One bed at a time was reported as a bug rather than met as a decision.** *In the kettle works i cant have something in the bed while the end of all gears has something in the bed.* A bed is a place, its cells are the puzzle and the wins a crop takes are the cost; restricting *which* town you may work adds a walk home before you may plant, which is bookkeeping. Every town's bed works now, and the Grower's node moved to `seed_cap` — how deep the drawer is — rather than being deleted, because a node that buys nothing is the thing M13.6 spent a milestone finding sixty of. | closed |
| 48 | **A companion had rows in the transcript and no row on the bar.** It fights *as gear*, which is M21.5's measured divergence, and its profiles reach the fight through `simulate_holding` rather than through the board — so `side_items`, which builds from the board, never saw them. Reported from play: *you currently cannot see your creature in the kennel during battle, but you can see their effects in the log.* They are rows with no cells now, which is what an innate attack already is on the other side of the screen, plus a line over your board saying who is out. **The sixth *a derived number needs somewhere it is shown*, and the first where the number was ticking correctly the whole time.** | closed |
| 49 | **The count lint was six nouns written out, and it missed three more the moment they existed.** `1 enchs`, `1 stacks`, `1 turns`, `1 curses`, `1 seconds`, `1 items` — and not `1 cells`, `1 points` or `1 times`. It asks the general question now: **one of anything followed by a word ending in s**, with the words that are not plural nouns asserted by name. Eighth hand-written list this project has paid for, and it was a lint that existed to catch exactly this class of thing. | closed |
| 50 | **Writing a promise out accurately proved one of them false.** The Grower's read *a row pays 120% of what it would — 4 instead of 4*, because the sentence worked the sum out with `* pct / 100` and `Game::harvest` rounds the **payer's** way: a row actually pays five. `plot::at_pct` is the one answer both go through. *A promise that disagrees with the sum it describes is worse than one that says nothing*, and the only reason anybody found out is that the sentence was made to carry the number. | closed |
| 51 | **Dropping a clause about nothing found a dead node.** FlashPowder shipped `pct: 0`, so both of its window nodes tuned a window that paid nothing — and `fp-longer-fuse` has **no prerequisite at all**, so a player's first point in that tree bought nought. `every_point_in_an_expert_tree_buys_something` passed it, because it measures a *fight* and the window never mattered in one; what caught it is `tuning_a_knob_changes_what_the_class_promises`, once the promise stopped printing `0%` and started saying nothing instead. **Two lints asking one question from two sides is how you find what one side cannot see** — which is the opposite of this file's usual finding about two lints, and worth having both ways round. | closed |
| 52 | **`no_class_on_offer_promises_a_stack` matched the word rather than the thing**, and refused the Hexweaver for saying *1 stack of its opposite* — a **curse** stack, the engine's own unit and precisely the number a player choosing that class is choosing. What may not be promised is a stack *of the class*, which is what upstream's *for each stack of Recycler you are carrying* was. And it held its own four-name copy of `class::OFFERED`, which moved into core in M10.2, so it had been asking about four of the seven ever since. | closed |
| 53 | **A pool had two names and the bar and the glossary each had one.** The bar said *mansus-sight* and *anticipation*; the glossary said *insight* and *dread*. That is the failure the character sheet settled years of blocks ago — *a node reading "start every fight with 12 armor" against a sheet reading "12 cork" is one number with two names* — and it came back the moment the mind lane reached a screen. A pool on a number screen is named the way the glossary names it now; the theme keeps the prose. TONE 13a is unchanged and what moved is which side of it a pool bar falls on. | closed |
| 54 | **The bed's pull went through `pickUp`, whose contract is *empty means it worked*.** It returned `'pulled'` to mean *handled*, so a successful pull took the refusal path — printed the word and returned **without refreshing** — and the crop stayed drawn on a bed it had already left. Reported from play. `onclaim` is the hook for *something else wants this click* and the board refreshes after it. **The bed had no browser check at all**, which is why it shipped: every other board in the game has one, and both added in this block were added because something in them broke first. | closed |
| 55 | **A check for a visual bug cannot ask the payload.** The first draft read `bed_json()` — core, which had harvested — and passed on the broken build. The bug is that the *canvas* disagrees with core, so the assertion has to be the board's own last read. `window.__bedDrawn()`. **What is drawn and what is true are two questions, and a visual bug is exactly the case where the answers differ.** | closed |
| 56 | **The plan's twenty-five figures are thirteen, and the difference is where they would be *shown*.** `PLAN-M21.md` §M21.10 lists eight crops, two growth stages, an ench seed, eight buyers, three benches and three papers. Recon: the bed, the run and the counter **draw themselves** — they are `Board`s, and a crop is already a coloured polyomino with a motif on it, so a crop figure and a bench figure have nowhere to go that is not decoration over a canvas that is already drawing the thing. What *had* nowhere at all was the people: eight buyers as eight rows of text, and **five specializations with no art of any kind** — the plan said three because it was written before the Grower, the Handler and the Factor existed. Thirteen figures from two drawings. Divergence recorded. | closed |
| 57 | **A specialization is not a paper, so it is not `expert.tex` with both seals the same.** Two identical blobs of wax read as an expert whose parents happen to match, which is a sentence about a thing this game does not have. The card is the paper's deliberate opposite number — landscape where that is portrait, hand-written where that is ruled, one seal where that has two, thumbed where that is folded — because a specialization is *one person on one map deciding to take you on* rather than an office filing something. | closed |
| 58 | **`make art`'s own rule caught both drawings, in one contact sheet.** *Draw it, then look at it* — three of the thirteen original family figures compiled cleanly and did not read, and so did these: the buyers' **arms were invisible**, drawn in the shadow colour *behind* the coat fill so the coat's own dark side swallowed them, and what each carried floated off in a corner rather than hanging from a hand. Then a second pass: an ear ring read as a blemish on the face and a bezier brim read as a flat black bar. **A figure that compiles is not a figure that works**, and the only way to know is to rasterise the set and put your eyes on it. | closed |
| 59 | **Four benches shipped and the glossary mentioned one.** M20 built the retort and M21 the bed, the run and the counter, and only the counter ever got a shelf — because the counter's was written in the same commit as the counter and the other three were not. That is *a derived number needs somewhere it is shown* at the **reference screen**: the glossary is the somewhere, and a system with no entry is a system a player can use and cannot look up. `every_bench_has_a_shelf` asks it over `Shown::bench` rather than over a list of four. | closed |
| 60 | **Writing the shelf found a fourth two-names-for-one-thing.** `Shown::bench` said *the retort* and the button, the screen heading and the glossary all say *the bench* — the retort is the **glass**, not the bench in the town. The lint caught it the instant the glossary had an entry to disagree with. Same failure as the pools on the fight bar, two commits earlier; **the way you find these is by giving the second surface something to say.** | closed |
| 61 | **Three lints' copies of *what a player can be* had gone stale, and all three surfaced in one afternoon.** `no_class_on_offer_promises_a_stack` held four names where `class::OFFERED` has seven; `every_class_the_game_offers_has_a_figure` read `OFFERED` plus the experts and called the five new cards *art shipped for nobody*; `every_class_a_player_can_be_is_in_the_glossary` asserted a count of 28. **A specialization is deliberately outside `OFFERED` because it pairs with nothing — and being outside that list is exactly how a thing goes unmentioned.** The Apothecary shipped with no door at all for the same reason. Worth stating as a rule: *a list that means "everything a player can be" is three lists in this game, and two of them are easy to forget.* | closed |
