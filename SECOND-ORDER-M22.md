# SECOND-ORDER-M22 — the notebook

Written as it goes. `M22.10` onward are read off this file: every row marked
`open` is work somebody has to do or a finding somebody has to write down, and
the last milestones of this block are whatever closes them.

Format is `SECOND-ORDER-M21.md`'s — numbered rows, a bold first sentence, and
`open` or `closed`. M21's closed at sixty-three rows with none open, so this
one starts at row 1 and nothing is carried in.

| # | row | state |
|---|---|---|
| 1 | **A one-tile wall is not a wall to a ball, and neither is a two-tile one.** `shot::shoot_with` tests the tile the tick *landed on* and never the tiles it crossed, and at `POWER_UNIT` 30 one tick is 1.875 tiles at power one and **18.75 at power ten** — so a ball passes straight through rock. Measured on the plan's draft cup: **6,546 of the shots taken from the 278 walkable tiles outside it come to rest inside it**, through eight tiles of solid rock with no mouth. A probe corridor with a one-column wall lets a ball through at seven of ten powers and a **two**-column wall at six of ten. No thickness seals it: nineteen tiles would be needed, which is the map. **`PLAN-M22.md` decision 9 — a sealed cup of rock — is not expressible in this engine**, and M22.0 is the milestone that exists to find that out before a map is authored. | open |
| 2 | **It has never cost anything, and that is why nobody found it.** Neither shipped table has a room to seal: the Treyway's twenty interior impassable tiles and the Undercountry's sixteen are all ranges along the border or three-wide blocks in open country, where a ball going through one reads as a lucky bounce. And **no ball ever comes to rest on impassable ground** — 0 of the 720-shot fan from every walkable tile of either table — because the destination tile *is* tested, so tunnelling only ever happens *through* something on the way to somewhere legal. A finding rather than a soft-lock. | open |
| 3 | **A swept collision seals the cup and costs two shipped maps a shot.** Prototyped: take the first solid tile the tick's segment crosses rather than the tile it ended on, sampled twice a tile. The draft cup goes from 6,546 ways in to **0**, the rest of the table stays whole in **2** rounds, and the flood costs 1.6s → 2.8s. What it breaks is four tests, and two of them are content: **`the-reach-edge` and `the-wextreen-reach` stop being reachable in one shot from the Treyway's start** — the way into the Wextreen Reach has been entered by tunnelling through a range since M17 — and `one_shot_reaches_most_of_the_treyway` falls from ≥80% to 78%. That is a player-visible change to a shipped map, made for a room `PlaceDef::to` already gives for free. **The decision is the human's** and the block does not take it. | open |
| 4 | **So the cup is a map rather than a room, and decision 8 is untouched.** The far pocket's `to` is a one-room map and its `at_to` is the tile you fall onto — which is exactly what decision 8 already builds, `warp_to(to, at_to)` in `Game::shoot`'s sunk arm, the same call the last-town arm already makes. From the player's side nothing about the design changes: aim at the far pocket, sink, and you are in the room with the thing in it, which is the plan's own Yoku citation. What it costs is one more map — **31 rather than 30** — and what it buys is that *the boss stands nowhere but the cup* is true by construction rather than by a lint over a flood. Divergence. | closed |
| 5 | **The casting count does not move, and the plan's 92 was the wrong shape of guess.** 89 of 106 are buyable today and **89** with High Wick's shelf reachable. The plan reasoned that three barrel-priced spells *leave* the cheap tier; they do, and they arrive on the shelf in the same move, so the net is zero. **Everything the arcane shelf stocks was already reachable through `barrel_pool` or `ledger_pool`** — a rerolled barrel could already produce all seventeen of its lines. What placing it buys is not availability, it is that the arcane components are somewhere a player can *find* rather than somewhere a reroll might produce. | open |
| 6 | **`common::from_save(common::THE_RUN)` cannot be the yardstick for a boss at this depth, because it beats nothing at this depth.** Measured: at level 45 the run is **974 health and 9 strength over 11 items**, against `geared_from`'s 1,942 and 30 over 21. It loses to the Undercountry's own boss (The Unwritten, 7.7s), to both M14 bosses, to the Tenth Surveyor and to all ten of the Wextreen deep; the only two things down there it beats are Gallowglass and Hollowmarch, both at 32–33 seconds. So *"a win between 20 and 28 seconds"* against it is a bracket only a creature rated **below the map's own pool** could satisfy. `CLAUDE.md` already says this in the Tenth Surveyor's own note — *the block is bracketed against the board that can actually reach her* — and `every_region_has_a_fight_you_can_win_and_every_boss_can_be_beaten` has required `geared_from` of every boss on a tile since M11.7. The boss is dressed against `geared_from`; the run is kept as the floor and asserted to lose. Divergence. | open |
| 7 | **Every fight `geared_from` wins at this depth is decided inside the sudden-death ramp.** The Unwritten 38.0s, the Ninth Surveyor 44.0s, What Marbulon Faced Away From 43.0s, the Tenth Surveyor 43.0s — and `SUDDEN_DEATH_MS` is 30,000, past which nothing survives about forty-four seconds. So the plan's *"a win between 20 and 28 seconds"* describes a fight two bands shallower: the only creature in the game that board beats in that window is **The Tailgate at 24.3s, rated 1336**, which is the sand cart's guard. The rule this project actually holds is the Tailgate's own — *a win at exactly `SUDDEN_DEATH_MS` is not a win a board earned* — so the bracket is a victory that is **not the buzzer's**, and the number to beat is The Unwritten's 38 seconds. | open |
| 8 | **The suite is 1,107 and the plan says 1,097.** `packaging/count-tests.sh` reads 1107 in 94 binaries against `4a3ae9a`, which is the commit the plan is written against. `MILESTONES.md` carries 1,097 from `c2baa10`, four commits earlier, and three commits have added tests since without moving it. The table in this block quotes the script. | open |
| 9 | **`git checkout` on a file with uncommitted work in it, again.** M21 row 4 wrote this down — *restore a negative test with a `cp` you took yourself, never `git checkout`* — and it happened anyway, one negative test into this block: reverting a break in `data.rs` took `shelves_on_the_map` out with it, because the whole file was uncommitted. The tell was a compile error naming a function that had existed a minute earlier. **A rule that is written down and is not a habit is a rule that will be broken by whoever wrote it down**, and the habit that actually holds is *copy the file before you break it*. | closed |
| 10 | **A check written against a compiled-in constant cannot be made to fail.** `every_shelf_is_a_town_a_wing_or_staged` passed with the wing arm of `shelves_on_the_map` replaced by `false &&`, because the shipped file has no wings yet and the answer is *the towns* whatever the rule says. That is `4a3ae9a`'s finding in a third costume — a check whose negative test cannot be made to fail through the check is a check nobody has proved. `shop::shelves_among(shops, towns)` is the rule with the file handed to it and `data::shelves_on_the_map` is the lookup, which is the same split `ShopsData::parse` and the lint make one field along. Both directions now break: a wing of a drawn town that is not reachable, and a wing of a staged one that is. | closed |
| 11 | **A wing needs a name and the plan costed two fields.** Every counter in the game takes its name from the map file it is drawn in and lets a theme override it by id; a wing is drawn in no map file. A name that lived only in `theme.rs` prints a hyphenated id under any theme that has not been told about it, which is the fall-through `place_name` already ends at. So `TownShelf` gains **three** fields — `wing_of`, `arrives`, `name` — `ShopsData::parse` refuses a nameless wing, and the shim still asks the theme first. Divergence. | closed |
| 12 | **Adding one predicate to `met` and not to `marks` would have made two answers out of one.** `place_is_there` had its own two-line closure and `marks()` is the same question as a list, read by `sealed_because`, `still_wanted`, `opens_onto`, `Floor::cleared`, the drains, `unlock::sentence` and `Requirement::Flag` — nine call sites. Teaching only the closure about `quests_done` would have left every one of those nine reading a shorter list, which is precisely *a rule with two homes is a rule with two answers*. Both learned it in the same commit and `met_is_marks_with_one_key_in_it` is what keeps them one question. | closed |
| 13 | **The shim was about to answer *which counter is here* three times.** `take_quest` compares where you are standing against an errand's `giver`, `hand_in_quest` against its `turn_in`, and `shop_json` against the town — and a wing's giver is the wing, so a desk standing in the third town would have had errands the guild listed and nobody could take. `shop::counters_at` is the one answer, in core, and the shim asks it once. Same shape as `quest::shown_at`, which was assembled in two crates until twenty-one chain errands could be taken and none handed in. | closed |
| 14 | **`ShopsData::parse` reads the map files, and decision 5 says it must not.** The barrel's *a barrel entry must not also be on a shelf* check has called `data::towns_on_the_map()` since M13, which reads every map's places. Teaching it about wings could not go through `data::shelves_on_the_map`, because that function parses this file and a parser that asks a parser about itself is a stack overflow — so the wings are worked out from the list already in hand. **The pre-existing violation is left where it is**: moving it to a lint is a change to a shipped refusal that this block has no reason to make, and it is written down here so the next person does not find it and assume it was an oversight. | open |
| 15 | **The new predicate opened a shipped door, and the check that caught it is a save somebody reported.** `quests_done` holds errand ids and `answered` holds event and place ids, and **the two namespaces collide**: the way under the Wextreen flat wants `the-tenth-survey`, which is the tenth surveyor's *sheet on a folding table*, and there is an errand of exactly that name. `CLAUDE.md` already records that collision as the reason a player stood on bare silt waiting for the dungeon to open — and teaching `met` to read errand ids bare made their confusion **true**: the reported save finished the errand, so the gate opened and `sealed_because` had nothing to say. `the_way_under_the_flat_is_shut_and_says_what_it_wants` failed, which is that save being a check paying for itself a block later. The read is prefixed `done:` now — **on the reading side, so there is no backfill**, which is the objection the plan raised against the writing side. `every_done_key_names_an_errand` refuses a prefix that points at nothing and asserts the one collision by name. | closed |
| 16 | **And `met` and `marks` failed it from two different sides.** With the bare read put back, `reefs` passes and `wings` fails: `sealed_because` reads `marks()` and the wing reads `met`, so a fix applied to one and not the other is invisible to whichever suite you happen to run. `met_is_marks_with_one_key_in_it` is the only thing that sees both, and it is the reason row 12 insisted they be one question. **Two lints asking one question from two sides is how you find what one side cannot see** — M21 row 51, from the other direction. | closed |
| 17 | **Ninety-four test binaries is fifty minutes on this machine, and it is not the tests.** A change to `crates/core` relinks every one of them, and `cargo test` interleaves building and running, so the transcript looks like a hang at whichever binary is next. Measured: 22 binaries in 11 minutes at a load average of 11, with the tests themselves at 0.06s each. `CLAUDE.md`'s own figure — *127 seconds relinking 83 binaries and 47 running* — was taken on an idle machine and this one has thirteen sessions, Steam, Discord and a Playwright Firefox left over from Wednesday. **And editing source while it runs restarts it**, which cost one whole pass before the cause was clear: the run is a snapshot of nothing, cargo notices and rebuilds. The working rule for this block is *one full suite a milestone, in the background, and `--test` while iterating.* | open |
| 18 | **Nothing in the Undercountry drops anything, so the clerk is sent for with a `Slay` and not a `Bring`.** `PLAN-M22.md` §M22.2 asks for *`Bring` of something the Undercountry drops*; `drops.json` has thirty entries and not one names a creature that stands down there. A `Bring` is answered by a component that already exists in the world, so it would have needed a new drop table entry — and a drop a player can farm is litter the moment the errand is done. A `Slay`'s token is created **by the errand**, cannot be got before it is asked for or after it is finished, and needs no `drops.json` at all. Divergence. | closed |
| 19 | **The token is a rumour the cut campaign left behind.** `A Word About the Cellar` is one of six one-cell components whose own comment says *what it does is stand as the condition on a door that will not otherwise be there* — on no shelf, granted by nothing, and unreachable since the fork. Using one makes it reachable content, which is exactly the move M20 made with four dead casting pieces, and it means **no new component and no fingerprint move**. Thirteen unused quest tokens remain; the next errand that wants proof of somewhere has twelve. | closed |
| 20 | **Three lints in `avail.rs` refused the first wing, and all three were right to.** A town sells something; a town is not the same shop as another town; every shelf is a town or staged. A wing is none of those things — the clerk's desk has errands and no stock at all — so each had to be re-asked rather than exempted: **a wing that sells nothing must want something** (a counter with neither is one nobody stands at), two shelves are compared only when both stock something (nought of nought is not one shop in two costumes), and the orphan sweep skips wings because *where a wing is written down is its host*. Same shape as `PlaceKind::Bench` growing a second kind of person in M21: a lint keyed to what a thing *was* refuses the first thing that is also something else. | closed |
| 21 | **The arcane shelf landing at the end of the game made the barrel undercut it, and the barrel is right.** The moment `high-wick` became a wing of a placed town, `on_a_shelf_you_can_reach` counted it and `ShopsData::parse` refused the file: *the barrel holds "Chapbook" and so does high-wick's shelf.* Three lines overlap — `Chapbook`, `Mirrorcast` and both `Rime Nova`s, which is exactly the *three of the five barrel-priced spells* the recon named. Taking them out of the **barrel** was measured and rejected: the only unclaimed replacements are `Emberburst` and `Siphon` at ratings 13 and 12 against the 8 and 5 they replace, which is +0.75 on a barrel mean that `the_barrel_is_a_floor_and_not_a_ceiling` already holds at 3.87 under the pit's 3.90 — so the opening barrel would have had to be re-tuned to protect a counter nobody reaches until level forty-five. **They came off the shelf instead**, and the reason is a design statement rather than a workaround: the shelf's own note called it *what makes a caster weapon buildable at all*, written when High Wick was a town somebody would reach early, and the thing that does that from the first afternoon is the barrel. 17 lines → 13. Divergence from *index order untouched* — and it costs nobody anything, because the shelf has been on no map since the fork so no `(id, index)` has ever been recorded. **It can only be free once, and this is the once.** | closed |
| 22 | **The way south is a `Door` in M22.3 and a `Gate` in M22.6, because a gate names a map.** `PLAN-M22.md` §M22.3 turns it into a gate with `to: the-lower-table` three milestones before that file exists — and `data::map` **falls back to the overworld** for a map this build has not got, so the gate would have led to West Bambulon and `every_gate_lands_beside_its_door` said so on the first run. `needs_all` is a `Door`'s field as much as a `Gate`'s, so the refusal, the naming and `sealed_because` all ship here and only the kind waits. Divergence. | closed |
| 23 | **A name is applied where the drains are, and eleven readers needed no change.** `PLAN-M22.md` decision 7 gives `World::name_of(p, state)` and one reader; the shim prints `p.name` in **eleven** places — the panel, the strip, the cart's timetable, the errand log's guide, the bestiary, the town's own header — and a second accessor would have meant finding all eleven and the compiler helping with none of them, because both are strings. `data::map_read_through` already rewrites the map the game sees: it applies the drains, and now it applies `named`. So *the file has the post with nothing on it and the game has the name*, which is `place_at`/`place_now`'s split exactly, and no reader moved. | closed |
| 24 | **Four lints asked *does this town trade* of the town's own shelf id, and a town whose counters are wings has none.** `every_town_stocks_something`, `towns_anywhere_in_the_world_all_trade_and_all_want_something`, `every_town_has_an_errand` and `the_third_town_has_no_shelves_and_says_so` all read `shops.town(id)` and `quests.at(id)`. The third town sells nothing and wants nothing under its own id and has two counters standing in it, which is what a player walks up to. All four ask about **the town and every wing of it** now — *whether or not the wing has arrived*, because that is a question about what the town is rather than about a particular afternoon. | closed |
| 25 | **The compiler caught the new `PlaceDef` field, and one test builds one by hand.** `primitives_m14.rs::sealed` writes out all twenty-six fields, so `named` was a build error rather than a silent `None` — which is `HANDOFF.md` trap 4's rule (*adding a field is a compile error until the save carries it*) working on a **test fixture**, where nobody had thought to want it. The alternative shape — `..Default::default()` — would have compiled and quietly given every hand-built place no name for ever. `PlaceDef` has no `Default` impl and should not grow one. | closed |
| 26 | **The shim keeps its own copy of `map_now`, and it drained but did not name.** `map_in` in `crates/wasm` is `data::map_read_through`'s twin — it exists because the shim caches the loaded maps and must not reload one a frame — and the two are the same job in two crates. That was harmless while the only thing either did was drain; the moment a place could be **named**, `cargo test` said the post was cut and the screen said it was blank. And the fast path made it worse: `if w.drains.is_empty()` handed the map out **uncloned**, and the third town's map has no drain on it. `World::read_by` is one body now and `World::reads_marks` is the fast path's question. **Found by the browser gate, which is the only thing that could have** — this is *a rule with two homes is a rule with two answers*, and the second home was in another crate. | closed |
| 27 | **I wrote the capped-slice bug that this file takes a deliberate effort not to write again.** `before = len(tape(page))` then `tape(page)[before:]` — `#tape` keeps **four** lines, so once four have been said the slice is empty and the comparison is about nothing. `CLAUDE.md` names it twice, once as *a slice of a capped list is a comparison that quietly stops being about anything* and once as the thing the town-mending check had to be rewritten to avoid, and it went in anyway on the first check that needed a refusal. The strip is four lines; read all four. | closed |
| 28 | **A probe that imports `testing/drive.py` runs the whole gate**, because `main()` is called at module level with no `__name__` guard. It bound the gate's fixed `PORT = 8127`, walked all hundred-odd checks against a **stale `dist/web`**, and printed its failures into the same transcript as the real run — so the fix looked like it had not worked when it had, and the next gate run died with *Address already in use*. Twenty minutes. The way to reuse a check is to read the source, strip the trailing `main()` and `exec` it with `__file__` set. | closed |
| 29 | **The walker waited sixty milliseconds for a shot that takes two and a half seconds to draw, and the third town is the first thing that made it matter.** `Game::shoot` runs the whole flight in core and the page *animates* it — `TICK_MS` 55 over four to forty-seven ticks — so `page.wait_for_timeout(60)` then reading `#coords` reads the tile the ball was fired **from**. On a table with nothing on it that is a wasted press. On a table with a **town** on it the shot lands on the counter, the town opens, the walker reads the old tile, marks the town unreachable, fires again, and does that until the press budget runs out: one run spent 160 shots at a town it was standing in. **The third town is the first town this game has ever put on a table**, so M22 is the first block that could have found it. `settle(page)` waits for the page to agree with core, which is exact and usually immediate. Fifty-three fights and a loop became seventy-eight and rising. | closed |
| 30 | **Three walker fixes before the real one, and each was a true finding about a screen that is not where the failure appeared.** A replay window of four seconds written for fights that end in two, against a level-45 board whose fights run thirty-eight to forty-four seconds of simulated time; a town that cannot be left because something opened over it while `#leave` was being retried; and the packing screen, which is `#fight` with the fight taken out of it and was being pressed *Fight* at. None of them was the loop, and all three are kept. **A fix that gets you further does not find fewer bugs; it finds the next one** — M21 row 63, four times in one afternoon. | closed |
| 31 | **The walker still does not take the third town's errands, and the reason is geography rather than a bug.** `send-for-the-clerk` is given and handed in at **Kettleworks**, which is the design — *an empty town cannot ask for its own clerk* — so walking the chain is four cart rides between two countries. The walker has no model of *go somewhere else to take an errand and come back*; it takes what is on the counter in front of it. So the chain is exercised by the browser gate, which plants each rung, and the walk is evidence about the **place** rather than about the chain. | open |
| 32 | **The packing board's canvas has no ceiling, and the barrel is what finds it.** `Board#fit` sizes the backing store to hold the whole bag — `bagY + rows * BAG_ROW + PAD` — and the barrel **never runs out**, which is the whole reason M12 has no save seam. The walk bought **3,216 components** off it over a hundred and forty-eight fights, the canvas reached **7,242 pixels**, and `#preset` — the Auto-pack button a player is *given* — went off the bottom of the screen and stopped being clickable. What that looks like from outside is a thirty-second Playwright timeout on a button that is visible, enabled and stable. A player who buys out the barrel every visit for an afternoon gets the same screen. **The finding is written down; the fix is a UI decision** — scroll the bag inside the board, paginate it, or cap the canvas and let the bag scroll under it — and it is not this block's. | open |
| 33 | **The walk is 148 fights from level 45 to 54, all of them in the Undercountry, and that is the evidence the block needed.** `The Morning Rush` 63, `Mumu Lelonde` 51, `The Sea of Cleveland` 27, `THE ONE WHO WENT DOWN` 7 — the pool's own weighting, easiest commonest, and the level-45 board wins every one of them. Board fill sat at 43–44% the whole way, bench 0. `testing/transcripts/m22.4-in-the-third-town.txt`. **It takes no errand**, for row 31's reason. | closed |
| 34 | **A gate with no `at_to` is a feature, and `PLAN-M22.md` decision 5 would have deleted it.** The plan says `parse` *refuses a `to` with no `at_to` on any kind*; the shipped maps refused to load on the first run, naming `the-door-in-the-wall`. A gate with no landing tile lands you **where you left off** — `WorldState::positions` remembers your tile on each map you have walked off and `World::arrival` falls through to it — and that is what makes the Treyway a country rather than a chute. The rule is a **pocket's**: a pocket is not a door you walk through, it is a hole you fall down, and falling down it twice has to put you in the same place both times. Divergence. | closed |
| 35 | **`a_pocket_that_goes_somewhere_costs_the_same_twelve` read the source and passed with the cost halved.** Its first draft counted `Contact::Sunk { .. } => POCKET_TIRES` in `shot.rs` and `POCKET_TIRES / 2` still contains that text. *A lint that reads a list rather than the behaviour is the failure it exists to catch, one level up* — written in the same afternoon as the rule it guards. It calls `Flight::tiring` on a flight that actually sank now, and breaks when the arm is halved. | closed |
| 36 | **And the tape check was vacuous in the way this project names.** It fired one shot at the pocket's own tile and wrapped every assertion in `if sunk`; that shot never sank, so the whole check was a *compares zero with zero* and stayed green with the second sentence deleted. It sweeps for a flight that sinks and asserts unconditionally. **Two of the four checks this milestone wrote were unproven until they were broken**, which is the argument for breaking every one. | closed |
| 37 | **`every_region_has_a_fight_you_can_win` refused the table's first pool, and the creature it named is rated *below* the one that replaced it.** The plan's four were to be chosen by damage a second; the first draft led with **Chainman at 1624**, which `common::geared_from` loses to at 37.8 seconds — while **Backsight at 1807** is a win at 39. `draw_enemy` makes a pool's easiest member its **commonest**, so Chainman would have been the draw a player meets most and the table would have been a wall. The pool is Backsight, The Eleventh Notch, Iron Under It and The Levelling Staff: the commonest is survivable and the other three kill, which is the Wextreen deep's own shape. **A rating predicts nothing about whether a fight is winnable** — this file's fourth instance, and the first where the *lower* rating was the harder fight. | closed |
| 38 | **`a_map_is_not_mostly_wallpaper` refused the cup's one card, and it was right.** One note and no questions is *reading it pays nothing*, on a map with exactly one thing to read. The card pays now — a weave out of the drift of everything else that came down the hole — which is also what makes it the right card: a room you fell into should tell you what else fell in. | closed |
| 39 | **And the card had to come off the tile the pocket drops you on.** `Game::warp_to` **resolves no place**, on purpose since M16 — every warp in the game lands you on ground, and a warp that opened a card would make a fall into a conversation. So the landing is [3, 3] and the drift is at [2, 1]. *An alcove is two tiles*, one map along, and it is the second time that rule has decided a layout. | closed |
| 40 | **The stop-line has moved twice and the rule has not.** M14 took it off the door under the lake and put it a tile south of the third town; M22.6 takes that tile for the way *on* and puts the sentence at the far end of the cup, behind the thing on the plank. `PlaceKind::Door` has one user before and after, `the_stop_line_is_still_one_door` and `there_is_one_door_in_the_game_and_it_is_the_last_thing` both say so, and the second had the map name written into it — which is how you find out a sentence has moved rather than been copied. | closed |
| 41 | **The table takes two rounds, which is the plan's guess and the Treyway's number**, and the far pocket is reachable. Written down as an assertion rather than left to the flood's `println`: if no shot reaches the far pocket the cup is a room with no way into it, and that is the whole of what M22.6 is for. | closed |

