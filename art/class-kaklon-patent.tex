% The Kaklon Patent — a licensee, not the man. Recycler: every assembly bonus
% counts for more, so the figure is somebody who finishes things rather than
% somebody who swings them. A bench, a component held up to the light, and an
% ench going onto it.
%
% Written by filling in tikz_figure_prompt.md.
\documentclass[tikz,border=4pt]{standalone}
\usetikzlibrary{calc}
\newcommand{\Unit}{0.055cm}
\newcommand{\Edge}{2.4pt}
\newcommand{\Bolts}{5}
\newcommand{\Sparks}{6}

\definecolor{coat}{HTML}{2E4A5A}
\definecolor{coatdark}{HTML}{1A2C36}
\definecolor{brass}{HTML}{C8952F}
\definecolor{plug}{HTML}{57B3C8}
\definecolor{bench}{HTML}{6B563A}
\definecolor{ink}{HTML}{101018}

\begin{document}
\begin{tikzpicture}[x=\Unit,y=\Unit, line join=round, line cap=round]

  % ---- the bench, which is the whole job ---------------------------------
  \fill[bench,draw=ink,line width=\Edge] (2,2) rectangle (62,12);
  \fill[coatdark,draw=ink,line width=1.6pt] (6,0) rectangle (12,3);
  \fill[coatdark,draw=ink,line width=1.6pt] (52,0) rectangle (58,3);
  % Bolts along the front edge, because a bench is a thing that was assembled.
  \foreach \i in {1,...,\Bolts} {
    \pgfmathsetmacro\x{8+(\i-1)*12}
    \fill[brass] (\x,7) circle (1.6);
  }

  % ---- legs, planted ------------------------------------------------------
  \fill[coatdark,draw=ink,line width=\Edge] (22,12) rectangle ++(9,10);
  \fill[coatdark,draw=ink,line width=\Edge] (34,12) rectangle ++(9,10);

  % ---- the coat -----------------------------------------------------------
  \fill[coat,draw=ink,line width=\Edge] (18,20) -- (48,20) -- (44,48) -- (22,48) -- cycle;
  \draw[coatdark,line width=1.8pt] (33,21) -- (33,47);
  \foreach \y in {26,33,40} { \fill[brass] (33,\y) circle (1.3); }

  % ---- arms: one holds the component up, one bolts the ench on -----------
  \draw[coat,line width=5.2pt] (22,44) -- (13,54);
  \draw[coat,line width=5.2pt] (44,44) -- (53,52);

  % ---- head, and the glass over one eye ----------------------------------
  \fill[coat,draw=ink,line width=\Edge] (33,54) circle (8);
  \fill[ink] (30,55) circle (1.5);
  \draw[brass,line width=2.0pt] (37,55) circle (3.4);
  \draw[brass,line width=1.6pt] (40,55) -- (43,56);

  % ---- the component, held up ---------------------------------------------
  \fill[bench,draw=ink,line width=\Edge] (5,54) rectangle ++(14,12);
  \draw[ink,line width=1.4pt] (12,54) -- (12,66);

  % ---- the ench, going on: a plug, and what comes off it ------------------
  \fill[plug,draw=ink,line width=\Edge] (49,50) rectangle ++(11,9);
  \draw[ink,line width=1.8pt] (53,59) -- (53,63);
  \draw[ink,line width=1.8pt] (57,59) -- (57,63);
  \foreach \i in {1,...,\Sparks} {
    \pgfmathsetmacro\a{20+(\i-1)*26}
    \draw[plug,line width=1.6pt]
      ({54.5+4*cos(\a)},{64+3*sin(\a)}) -- ({54.5+7*cos(\a)},{64+5.5*sin(\a)});
  }

\end{tikzpicture}
\end{document}
